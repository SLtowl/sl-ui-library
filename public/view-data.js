// Generated route markup and initial examples. No navigation fetch is required.
export const views = {"home":"<main id=\"main\" class=\"catalog-main category-home\" tabindex=\"-1\">\n    <div class=\"home-intro\">\n      <h1>Components</h1>\n      <p>Choose a type to explore its components.</p>\n    </div>\n    <nav class=\"category-tiles\" id=\"category-tiles\" aria-label=\"Component categories\"></nav>\n    <noscript><p><a href=\"./component.html?component=matte-save-cancel\">Open Save &amp; Cancel</a>. Enable JavaScript to browse all categories.</p></noscript>\n  </main>\n<dialog id=\"usage-dialog\" aria-labelledby=\"usage-title\">\n    <div class=\"dialog-header\"><h2 id=\"usage-title\">Using the library</h2><button id=\"close-usage\" class=\"close-button\" type=\"button\" aria-label=\"Close usage\"><svg viewBox=\"0 0 24 24\" fill=\"none\" aria-hidden=\"true\"><path d=\"m6 6 12 12M18 6 6 18\"/></svg></button></div>\n    <div class=\"usage-content\">\n      <section><h3>Start with a type</h3><p>Choose an icon tile on the home page to open a component category. Inside that category, search by name, action or motion.</p></section>\n      <section><h3>Try it before you take it</h3><p>Use a component’s live preview, then open it to see its HTML, CSS and JavaScript. Select the part you need and copy a file or download the complete zip.</p></section>\n      <section><h3>Keep structure and motion separate</h3><p>A component belongs to one type. Different motion treatments stay inside that type. Pair, Save and Cancel are export options for one component, not three separate components.</p></section>\n      <section><h3>Bring it into your project</h3><p>The current component uses plain HTML, CSS and JavaScript with a bundled font. Its download works offline. Edit the CSS variables and connect the actions to your own logic.</p></section>\n    </div>\n  </dialog>","category":"<main id=\"main\" class=\"catalog-main category-page\" tabindex=\"-1\">\n    <div class=\"category-intro\">\n      <div><div class=\"category-title\"><a class=\"category-return\" href=\"./\" aria-label=\"Back to component categories\" title=\"Browse categories\"><svg viewBox=\"0 0 24 24\" fill=\"none\" aria-hidden=\"true\"><rect x=\"3\" y=\"3\" width=\"7\" height=\"7\" rx=\"2\"/><rect x=\"14\" y=\"3\" width=\"7\" height=\"7\" rx=\"2\"/><rect x=\"3\" y=\"14\" width=\"7\" height=\"7\" rx=\"2\"/><rect x=\"14\" y=\"14\" width=\"7\" height=\"7\" rx=\"2\"/></svg></a><h1 id=\"category-heading\">Components</h1></div><p id=\"category-description\"></p></div>\n      <form class=\"catalog-search\" role=\"search\" aria-label=\"Search this category\">\n        <button id=\"search-toggle\" type=\"button\" aria-label=\"Search components\" title=\"Search components\" aria-expanded=\"false\" aria-controls=\"search-field\"><svg viewBox=\"0 0 24 24\" fill=\"none\" aria-hidden=\"true\"><circle cx=\"10.8\" cy=\"10.8\" r=\"6.8\"/><path d=\"m16 16 4.5 4.5\"/></svg></button>\n        <div id=\"search-field\" class=\"search-field\" inert>\n          <label class=\"visually-hidden\" for=\"component-search\">Search this category</label>\n          <input id=\"component-search\" type=\"search\" placeholder=\"Search components…\" autocomplete=\"off\" maxlength=\"160\" spellcheck=\"false\">\n          <button id=\"clear-search\" type=\"button\" aria-label=\"Clear search\" hidden><svg viewBox=\"0 0 24 24\" fill=\"none\" aria-hidden=\"true\"><path d=\"m7 7 10 10M17 7 7 17\"/></svg></button>\n        </div>\n      </form>\n    </div>\n    <section class=\"catalog-results\" aria-labelledby=\"category-heading\">\n      <p id=\"result-count\" class=\"result-count\"></p>\n      <div id=\"component-grid\" class=\"component-grid\"></div>\n      <div id=\"catalog-empty\" class=\"catalog-empty\" hidden>\n        <h2 id=\"empty-title\">No components here yet</h2>\n        <p id=\"empty-description\"></p>\n        <button id=\"clear-search-empty\" class=\"surface-button\" type=\"button\" hidden>Clear search</button>\n        <a id=\"empty-back\" class=\"surface-button\" href=\"./\">Explore other categories</a>\n      </div>\n      <p class=\"visually-hidden\" id=\"catalog-status\" role=\"status\" aria-live=\"polite\" aria-atomic=\"true\"></p>\n    </section>\n    <noscript><p><a href=\"./\">Browse categories</a> or <a href=\"./component.html?component=matte-save-cancel\">open Save &amp; Cancel</a>. Enable JavaScript to see the list of components.</p></noscript>\n  </main>\n<dialog id=\"usage-dialog\" aria-labelledby=\"usage-title\">\n    <div class=\"dialog-header\"><h2 id=\"usage-title\">Using the library</h2><button id=\"close-usage\" class=\"close-button\" type=\"button\" aria-label=\"Close usage\"><svg viewBox=\"0 0 24 24\" fill=\"none\" aria-hidden=\"true\"><path d=\"m6 6 12 12M18 6 6 18\"/></svg></button></div>\n    <div class=\"usage-content\">\n      <section><h3>Start with a type</h3><p>Choose an icon tile on the home page to open a component category. Inside that category, search by name, action or motion.</p></section>\n      <section><h3>Try it before you take it</h3><p>Use a component’s live preview, then open it to see its HTML, CSS and JavaScript. Select the part you need and copy a file or download the complete zip.</p></section>\n      <section><h3>Keep structure and motion separate</h3><p>A component belongs to one type. Different motion treatments stay inside that type. Pair, Save and Cancel are export options for one component, not three separate components.</p></section>\n      <section><h3>Bring it into your project</h3><p>The current component uses plain HTML, CSS and JavaScript with a bundled font. Its download works offline. Edit the CSS variables and connect the actions to your own logic.</p></section>\n    </div>\n  </dialog>","component":"<main id=\"main\" tabindex=\"-1\">\n    <div class=\"intro\">\n      <h1>Save &amp; Cancel</h1>\n      <p>Matte buttons with smooth state changes.</p>\n    </div>\n    <section class=\"workbench\" aria-label=\"Save and Cancel component\">\n      <div class=\"workbench-toolbar\">\n        <span class=\"single-component-label\" hidden></span>\n        <div class=\"variant-controls\" role=\"group\" aria-label=\"Buttons to preview and download\">\n          <button type=\"button\" data-variant=\"pair\" aria-pressed=\"true\">Pair</button>\n          <button type=\"button\" data-variant=\"save\" aria-pressed=\"false\">Save</button>\n          <button type=\"button\" data-variant=\"cancel\" aria-pressed=\"false\">Cancel</button>\n        </div>\n        <div class=\"workbench-actions\">\n          <button id=\"reset\" class=\"reset-button\" type=\"button\" aria-label=\"Reset button animation\">\n            <svg viewBox=\"0 0 24 24\" fill=\"none\" aria-hidden=\"true\"><path d=\"M20 8a9 9 0 1 0 1 7M20 3v5h-5\"/></svg>\n            <span>Reset</span>\n          </button>\n          <a class=\"surface-button download-button\" id=\"download\" href=\"./downloads/matte-pair.zip\" download=\"matte-pair.zip\">Download .zip</a>\n        </div>\n      </div>\n      <div class=\"workbench-body\">\n        <section class=\"preview-panel\" aria-labelledby=\"preview-heading\">\n          <h2 id=\"preview-heading\">Preview</h2>\n          <sl-preview id=\"component-preview\" role=\"group\" aria-label=\"Interactive component preview\"></sl-preview>\n        </section>\n        <section class=\"code-panel\" aria-label=\"Component source code\">\n          <div class=\"code-toolbar\">\n            <div class=\"file-tabs\" role=\"tablist\" aria-label=\"Source language\">\n              <button id=\"html-tab\" type=\"button\" role=\"tab\" aria-selected=\"false\" aria-controls=\"source-panel\" tabindex=\"-1\" data-file=\"index.html\">HTML</button>\n              <button id=\"css-tab\" type=\"button\" role=\"tab\" aria-selected=\"false\" aria-controls=\"source-panel\" tabindex=\"-1\" data-file=\"buttons.css\">CSS</button>\n              <button id=\"js-tab\" type=\"button\" role=\"tab\" aria-selected=\"true\" aria-controls=\"source-panel\" data-file=\"example.js\">JavaScript</button>\n            </div>\n            <button class=\"surface-button copy-button\" id=\"copy\" type=\"button\" disabled>\n              <svg viewBox=\"0 0 24 24\" fill=\"none\" aria-hidden=\"true\"><rect x=\"7\" y=\"3\" width=\"14\" height=\"17\" rx=\"2.5\"/><path d=\"M17 20v1a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h2\"/></svg>\n              <span id=\"copy-label\">Copy code</span>\n            </button>\n          </div>\n          <div id=\"source-panel\" role=\"tabpanel\" aria-labelledby=\"js-tab\" tabindex=\"0\" aria-busy=\"true\">\n            <pre class=\"line-numbers\" id=\"line-numbers\" aria-hidden=\"true\">1</pre>\n            <pre class=\"source-pre\"><code id=\"source-code\">Loading source…</code></pre>\n          </div>\n          <p id=\"copy-status\" role=\"status\" aria-live=\"polite\" aria-atomic=\"true\"></p>\n        </section>\n      </div>\n    </section>\n  </main>\n<dialog id=\"usage-dialog\" aria-labelledby=\"usage-title\">\n    <div class=\"dialog-header\">\n      <h2 id=\"usage-title\">Make it yours</h2>\n      <button id=\"close-usage\" class=\"close-button\" type=\"button\" aria-label=\"Close usage\"><svg viewBox=\"0 0 24 24\" fill=\"none\" aria-hidden=\"true\"><path d=\"m6 6 12 12M18 6 6 18\"/></svg></button>\n    </div>\n    <div class=\"usage-content\">\n      <section><h3>Take the component</h3><p>Choose Pair, Save or Cancel, then download the zip. Unpack it and open <code>index.html</code>. The example works offline, with no dependencies.</p></section>\n      <section><h3>Connect your action</h3><p>In <code>example.js</code>, connect <code>onSave</code> to your async save function and <code>onCancel</code> to your close or discard action. Pass the supplied <code>signal</code> to abortable requests.</p><p>The button shows Saved when your promise resolves. Cancel aborts the pending operation. A failed save is announced to assistive technology; handle visible error messaging in your project.</p><p>The preview uses a 1.9-second simulated request in <code>preview.js</code>; it sends no data. Remove <code>previewDuration</code> when connecting a real request. There is no fixed loading delay in the component.</p></section>\n      <section><h3>Adjust the look</h3><p>Edit the <code>.button-kit</code> variables in <code>buttons.css</code> for colors, sizing and timing. The included Instrument Sans font is covered by <code>OFL.txt</code>. Keyboard focus and reduced-motion preferences are supported.</p></section>\n      <div class=\"usage-actions\"><button class=\"surface-button\" id=\"show-integration\" type=\"button\">View integration</button><button class=\"text-button\" id=\"show-controller\" type=\"button\">View full JavaScript</button></div>\n    </div>\n  </dialog>"};
export const initialSources = {"pair/example.js":"const root = document.querySelector('.button-kit');\n\nMatteButtons.mount(root, {\n  onSave: saveChanges,\n  onCancel: closeEditor,\n  previewDuration: 1900\n});\n","save/example.js":"const root = document.querySelector('.button-kit');\n\nMatteButtons.mount(root, {\n  onSave: saveChanges,\n  previewDuration: 1900\n});\n","cancel/example.js":"const root = document.querySelector('.button-kit');\n\nMatteButtons.mount(root, {\n  onCancel: closeEditor\n});\n","copy/example.js":"const button = document.querySelector('.sl-copy');\n\nMatteCopy.mount(button, {\n  getText: () => 'SL UI Library', // Return your own text here.\n  feedback: document.querySelector('.copy-feedback'),\n  holdMs: 1700\n});\n","like/example.js":"const button = document.querySelector('.sl-like');\n\nconst like = MatteLike.mount(button, {\n  feedback: document.querySelector('.like-feedback')\n});\n\nbutton.addEventListener('likechange', event => {\n  // event.detail.pressed is the new local state.\n  // Connect your own persistence here if your project needs it.\n});\n","download/example.js":"const button = document.querySelector('.sl-download');\n\n// This transferable example starts a real browser download.\n// The library uses a separate animation-only demo with no downloaded file.\nconst download = MatteDownload.mount(button, {\n  feedback: document.querySelector('.download-feedback'),\n  getFile: ({ signal }) => {\n    if (signal.aborted) throw new DOMException('Cancelled', 'AbortError');\n    // Replace this sample with your Blob or async file request.\n    return {\n      blob: new Blob(['SL UI Library sample'], { type: 'text/plain;charset=utf-8' }),\n      filename: 'sl-ui-sample.txt'\n    };\n  }\n});\n","bookmark/example.js":"const button = document.querySelector('.sl-bookmark');\n\nconst bookmark = MatteBookmark.mount(button, {\n  feedback: document.querySelector('.bookmark-feedback')\n});\n\nbutton.addEventListener('bookmarkchange', event => {\n  // event.detail.pressed is the new local state.\n  // Connect your storage here; setPressed(value) can restore saved state.\n});\n","playback/example.js":"const button = document.querySelector('.sl-playback');\n\nconst playback = MattePlayback.mount(button, {\n  feedback: document.querySelector('.playback-feedback'),\n  onChange: ({ playing }) => {\n    // Connect your player here. This sample changes local state only.\n    // If media.play() rejects, call playback.setPlaying(false).\n  }\n});\n","delete/example.js":"const button = document.querySelector('.sl-delete');\n\nconst deletion = MatteDelete.mount(button, {\n  feedback: document.querySelector('.delete-feedback'),\n  onDelete: async ({ signal }) => {\n    if (signal.aborted) throw new DOMException('Canceled', 'AbortError');\n    // Working local example: remove only the sample text in index.html.\n    // Replace this action with your own deletion and await its result.\n    document.querySelector('[data-delete-example]')?.remove();\n  }\n});\n\n// A single click starts onDelete immediately. Your application is responsible\n// for confirmation or undo when appropriate.\n// reset() resets the control; it does not restore deleted data.\n// Call deletion.destroy() before removing the button.\n","upload/example.js":"const button = document.querySelector('.sl-upload');\nconst input = document.querySelector('input[type=\"file\"]');\n\n// Replace undefined with your async function:\n// onUpload(file, { signal, onProgress })\n// Resolve only after your upload succeeds, reject on failure, and report\n// progress as a fraction from 0 to 1. Pass signal to your request.\n// No endpoint is assumed. Without a handler, this example selects a local\n// file without reading or uploading its contents.\nconst onUpload = undefined;\n\nconst upload = MatteUpload.mount(button, {\n  input,\n  feedback: document.querySelector('.upload-feedback'),\n  onUpload\n});\n\n// upload.select(file) accepts an existing File from your application.\n// upload.reset() aborts pending work; destroy() also removes listeners.\n","share/example.js":"const share = MatteShare.mount(document.querySelector('.sl-share-control'), {\n  getUrl: () => 'https://example.com/article',\n  feedback: document.querySelector('.share-feedback'),\n  labels: {\n    idle: 'Share', copy: 'Copy link', copied: 'Link copied',\n    sharing: 'Sharing', shared: 'Shared', error: 'Try again', send: 'Share…'\n  }\n});\n\n// This working example uses the browser's native Web Share API when available.\n// Copy link remains a separate clipboard action.\n// To connect your own sharing service, supply onShare({ url, signal }).\n// Resolve after the action succeeds. Return { label: 'Sent' } to confirm\n// a completed send, or { status: 'canceled' } when the user cancels.\n// The library's simulated sharing handler is not included in this example.\n// Call share.destroy() before removing the component.\n","follow/example.js":"// A small local model for this example. Replace it with your application state.\nconst profile = { following: false };\nconst follow = MatteFollow.mount(document.querySelector('.sl-follow'), {\n  feedback: document.querySelector('.follow-feedback'),\n  onChange: following => {\n    profile.following = following;\n    // Connect your application's follow/unfollow action here.\n    // If it fails, restore the previous state with follow.setFollowing(value).\n  }\n});\nfollow.setFollowing(profile.following);\n\n// onChange receives a boolean after a user action. The followchange event\n// also exposes event.detail.following. setFollowing() and reset() are silent.\n// Call follow.destroy() before removing the button.\n","text-input/example.js":"const field = MatteTextInput.mount(document.querySelector('#text-field'));\n\n// Read the native input value in your form or application.\n// field.reset() restores its default value; destroy() removes enhancements.\n","search-input/example.js":"const field = document.querySelector('#search-field');\nconst search = MatteSearchInput.mount(field, {\n  items: ['Buttons', 'Inputs', 'Toggles', 'Checkboxes', 'Sliders', 'Menus']\n});\n\nfield.addEventListener('searchselect', event => {\n  // event.detail.value is the selected local item.\n});\n","email-input/example.js":"const email = MatteEmailInput.mount(document.querySelector('#email-field'));\n\n// Call email.validate() before submitting your form if needed.\n// The native input validity API remains available.\n","password-input/example.js":"const password = MattePasswordInput.mount(document.querySelector('#password-field'));\n\n// password.setVisible(false) conceals the native input without moving its caret.\n// Keep passwords out of logs and local preview data in your application.\n","number-input/example.js":"const quantity = MatteNumberInput.mount(document.querySelector('#number-field'));\n\n// quantity.stepUp() and stepDown() use the native min, max and step values.\n// quantity.validate() reports the current native validity state.\n","textarea-input/example.js":"const notes = MatteTextareaInput.mount(document.querySelector('#textarea-field'));\n\n// Read the native textarea value as usual.\n// The control grows to its configured maximum height and then remains scrollable.\n","url-input/example.js":"const website = MatteUrlInput.mount(document.querySelector('#url-field'));\n\n// website.validate() checks the native URL syntax and requires http:// or https://.\n// Verify and sanitize URLs again wherever your application consumes them.\n","phone-input/example.js":"const phone = MattePhoneInput.mount(document.querySelector('#phone-field'));\n\n// Common +1, +7, +33, +44 and +49 prefixes have display masks.\n// Keep the underlying value handling aligned with the countries your product supports.\n","amount-input/example.js":"const amount = MatteAmountInput.mount(document.querySelector('#amount-field'), {\n  locale: 'en-US',\n  minimumFractionDigits: 0,\n  maximumFractionDigits: 2\n});\n\ndocument.querySelector('#amount-field').addEventListener('amountchange', event => {\n  // event.detail.amount contains the parsed number.\n});\n","code-input/example.js":"const code = MatteCodeInput.mount(document.querySelector('#code-field'), {\n  expectedCode: '482731' // Demo only. Verify real codes on your server.\n});\n\ndocument.querySelector('#code-field').addEventListener('codecomplete', event => {\n  // event.detail contains { code, valid } for this local demonstration.\n});\n","glide-toggle/example.js":"const root = document.querySelector('.sl-toggle');\nconst toggle = MatteToggle.mount(root, {\n  onChange(checked) {\n    // Connect this boolean to your application's setting.\n  }\n});\n// toggle.setChecked(value) updates state silently.\n// toggle.reset() restores the default; toggle.destroy() removes listeners.\n","theme-toggle/example.js":"const root = document.querySelector('.sl-toggle');\nconst toggle = MatteToggle.mount(root, {\n  onChange(checked) {\n    // Connect this boolean to your application's setting.\n  }\n});\n// toggle.setChecked(value) updates state silently.\n// toggle.reset() restores the default; toggle.destroy() removes listeners.\n","sound-toggle/example.js":"const root = document.querySelector('.sl-toggle');\nconst toggle = MatteToggle.mount(root, {\n  onChange(checked) {\n    // Connect this boolean to your application's setting.\n  }\n});\n// toggle.setChecked(value) updates state silently.\n// toggle.reset() restores the default; toggle.destroy() removes listeners.\n","privacy-toggle/example.js":"const root = document.querySelector('.sl-toggle');\nconst toggle = MatteToggle.mount(root, {\n  onChange(checked) {\n    // Connect this boolean to your application's setting.\n  }\n});\n// toggle.setChecked(value) updates state silently.\n// toggle.reset() restores the default; toggle.destroy() removes listeners.\n","sync-toggle/example.js":"const root = document.querySelector('.sl-toggle');\nconst toggle = MatteToggle.mount(root, {\n  onChange(checked) {\n    // Connect this boolean to your application's setting.\n  }\n});\n// toggle.setChecked(value) updates state silently.\n// toggle.reset() restores the default; toggle.destroy() removes listeners.\n","wifi-toggle/example.js":"const root = document.querySelector('.sl-toggle');\nconst toggle = MatteToggle.mount(root, {\n  onChange(checked) {\n    // Connect this boolean to your application's setting.\n  }\n});\n// toggle.setChecked(value) updates state silently.\n// toggle.reset() restores the default; toggle.destroy() removes listeners.\n","location-toggle/example.js":"const root = document.querySelector('.sl-toggle');\nconst toggle = MatteToggle.mount(root, {\n  onChange(checked) {\n    // Connect this boolean to your application's setting.\n  }\n});\n// toggle.setChecked(value) updates state silently.\n// toggle.reset() restores the default; toggle.destroy() removes listeners.\n","battery-toggle/example.js":"const root = document.querySelector('.sl-toggle');\nconst toggle = MatteToggle.mount(root, {\n  onChange(checked) {\n    // Connect this boolean to your application's setting.\n  }\n});\n// toggle.setChecked(value) updates state silently.\n// toggle.reset() restores the default; toggle.destroy() removes listeners.\n","microphone-toggle/example.js":"const root = document.querySelector('.sl-toggle');\nconst toggle = MatteToggle.mount(root, {\n  onChange(checked) {\n    // Connect this boolean to your application's setting.\n  }\n});\n// toggle.setChecked(value) updates state silently.\n// toggle.reset() restores the default; toggle.destroy() removes listeners.\n","autosave-toggle/example.js":"const root = document.querySelector('.sl-toggle');\nconst toggle = MatteToggle.mount(root, {\n  onChange(checked) {\n    // Connect this boolean to your application's setting.\n  }\n});\n// toggle.setChecked(value) updates state silently.\n// toggle.reset() restores the default; toggle.destroy() removes listeners.\n","draw-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","fill-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","sweep-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","task-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","group-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","cross-square-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","cross-circle-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","star-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","heart-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","bookmark-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","shield-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","pin-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","bell-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","ring-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","corner-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","chips-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","days-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","tiles-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","swatches-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","round-list-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","exclude-list-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","feature-list-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","file-list-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","table-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","nested-checkbox/example.js":"const root = document.querySelector('.sl-check-card');\nconst selection = MatteCheckboxes.mount(root, {\n  onChange(values) {\n    // Connect selected input names to your application.\n  }\n});\n// selection.values reads the selected names.\n// selection.reset() restores defaults; selection.destroy() removes listeners.\n","volume-slider/example.js":"const root = document.querySelector('.mixer-group, .sl-slider');\nconst slider = MatteSlider.mount(root, {\n  onChange(values) {\n    // Connect numeric values to your application.\n  }\n});\n// slider.values reads the current values.\n// slider.reset() restores defaults; slider.destroy() removes listeners.\n","brightness-slider/example.js":"const root = document.querySelector('.mixer-group, .sl-slider');\nconst slider = MatteSlider.mount(root, {\n  onChange(values) {\n    // Connect numeric values to your application.\n  }\n});\n// slider.values reads the current values.\n// slider.reset() restores defaults; slider.destroy() removes listeners.\n","steps-slider/example.js":"const root = document.querySelector('.mixer-group, .sl-slider');\nconst slider = MatteSlider.mount(root, {\n  onChange(values) {\n    // Connect numeric values to your application.\n  }\n});\n// slider.values reads the current values.\n// slider.reset() restores defaults; slider.destroy() removes listeners.\n","range-slider/example.js":"const root = document.querySelector('.mixer-group, .sl-slider');\nconst slider = MatteSlider.mount(root, {\n  onChange(values) {\n    // Connect numeric values to your application.\n  }\n});\n// slider.values reads the current values.\n// slider.reset() restores defaults; slider.destroy() removes listeners.\n","vertical-slider/example.js":"const root = document.querySelector('.mixer-group, .sl-slider');\nconst slider = MatteSlider.mount(root, {\n  onChange(values) {\n    // Connect numeric values to your application.\n  }\n});\n// slider.values reads the current values.\n// slider.reset() restores defaults; slider.destroy() removes listeners.\n","scrub-slider/example.js":"const root = document.querySelector('.mixer-group, .sl-slider');\nconst slider = MatteSlider.mount(root, {\n  onChange(values) {\n    // Connect numeric values to your application.\n  }\n});\n// slider.values reads the current values.\n// slider.reset() restores defaults; slider.destroy() removes listeners.\n","speed-slider/example.js":"const root = document.querySelector('.mixer-group, .sl-slider');\nconst slider = MatteSlider.mount(root, {\n  onChange(values) {\n    // Connect numeric values to your application.\n  }\n});\n// slider.values reads the current values.\n// slider.reset() restores defaults; slider.destroy() removes listeners.\n","zoom-slider/example.js":"const root = document.querySelector('.mixer-group, .sl-slider');\nconst slider = MatteSlider.mount(root, {\n  onChange(values) {\n    // Connect numeric values to your application.\n  }\n});\n// slider.values reads the current values.\n// slider.reset() restores defaults; slider.destroy() removes listeners.\n","temperature-slider/example.js":"const root = document.querySelector('.mixer-group, .sl-slider');\nconst slider = MatteSlider.mount(root, {\n  onChange(values) {\n    // Connect numeric values to your application.\n  }\n});\n// slider.values reads the current values.\n// slider.reset() restores defaults; slider.destroy() removes listeners.\n","balance-slider/example.js":"const root = document.querySelector('.mixer-group, .sl-slider');\nconst slider = MatteSlider.mount(root, {\n  onChange(values) {\n    // Connect numeric values to your application.\n  }\n});\n// slider.values reads the current values.\n// slider.reset() restores defaults; slider.destroy() removes listeners.\n","exposure-slider/example.js":"const root = document.querySelector('.mixer-group, .sl-slider');\nconst slider = MatteSlider.mount(root, {\n  onChange(values) {\n    // Connect numeric values to your application.\n  }\n});\n// slider.values reads the current values.\n// slider.reset() restores defaults; slider.destroy() removes listeners.\n","segments-slider/example.js":"const root = document.querySelector('.mixer-group, .sl-slider');\nconst slider = MatteSlider.mount(root, {\n  onChange(values) {\n    // Connect numeric values to your application.\n  }\n});\n// slider.values reads the current values.\n// slider.reset() restores defaults; slider.destroy() removes listeners.\n","rating-slider/example.js":"const root = document.querySelector('.mixer-group, .sl-slider');\nconst slider = MatteSlider.mount(root, {\n  onChange(values) {\n    // Connect numeric values to your application.\n  }\n});\n// slider.values reads the current values.\n// slider.reset() restores defaults; slider.destroy() removes listeners.\n","hours-slider/example.js":"const root = document.querySelector('.mixer-group, .sl-slider');\nconst slider = MatteSlider.mount(root, {\n  onChange(values) {\n    // Connect numeric values to your application.\n  }\n});\n// slider.values reads the current values.\n// slider.reset() restores defaults; slider.destroy() removes listeners.\n","distance-slider/example.js":"const root = document.querySelector('.mixer-group, .sl-slider');\nconst slider = MatteSlider.mount(root, {\n  onChange(values) {\n    // Connect numeric values to your application.\n  }\n});\n// slider.values reads the current values.\n// slider.reset() restores defaults; slider.destroy() removes listeners.\n","meter-slider/example.js":"const root = document.querySelector('.mixer-group, .sl-slider');\nconst slider = MatteSlider.mount(root, {\n  onChange(values) {\n    // Connect numeric values to your application.\n  }\n});\n// slider.values reads the current values.\n// slider.reset() restores defaults; slider.destroy() removes listeners.\n","mixer-slider/example.js":"const root = document.querySelector('.mixer-group, .sl-slider');\nconst slider = MatteSlider.mount(root, {\n  onChange(values) {\n    // Connect numeric values to your application.\n  }\n});\n// slider.values reads the current values.\n// slider.reset() restores defaults; slider.destroy() removes listeners.\n","type-size-slider/example.js":"const root = document.querySelector('.mixer-group, .sl-slider');\nconst slider = MatteSlider.mount(root, {\n  onChange(values) {\n    // Connect numeric values to your application.\n  }\n});\n// slider.values reads the current values.\n// slider.reset() restores defaults; slider.destroy() removes listeners.\n","radius-slider/example.js":"const root = document.querySelector('.mixer-group, .sl-slider');\nconst slider = MatteSlider.mount(root, {\n  onChange(values) {\n    // Connect numeric values to your application.\n  }\n});\n// slider.values reads the current values.\n// slider.reset() restores defaults; slider.destroy() removes listeners.\n","opacity-slider/example.js":"const root = document.querySelector('.mixer-group, .sl-slider');\nconst slider = MatteSlider.mount(root, {\n  onChange(values) {\n    // Connect numeric values to your application.\n  }\n});\n// slider.values reads the current values.\n// slider.reset() restores defaults; slider.destroy() removes listeners.\n","actions-menu/example.js":"const root = document.querySelector('.menu-demo');\nconst menu = MatteMenu.mount(root, {\n  onSelect(values) {\n    // Connect these values to your application.\n  }\n});\n// menu.reset() restores defaults; menu.destroy() removes listeners.\n","sort-menu/example.js":"const root = document.querySelector('.menu-demo');\nconst menu = MatteMenu.mount(root, {\n  onSelect(values) {\n    // Connect these values to your application.\n  }\n});\n// menu.reset() restores defaults; menu.destroy() removes listeners.\n","labels-menu/example.js":"const root = document.querySelector('.menu-demo');\nconst menu = MatteMenu.mount(root, {\n  onSelect(values) {\n    // Connect these values to your application.\n  }\n});\n// menu.reset() restores defaults; menu.destroy() removes listeners.\n","context-menu/example.js":"const root = document.querySelector('.menu-demo');\nconst menu = MatteMenu.mount(root, {\n  onSelect(values) {\n    // Connect these values to your application.\n  }\n});\n// menu.reset() restores defaults; menu.destroy() removes listeners.\n","commands-menu/example.js":"const root = document.querySelector('.menu-demo');\nconst menu = MatteMenu.mount(root, {\n  onSelect(values) {\n    // Connect these values to your application.\n  }\n});\n// menu.reset() restores defaults; menu.destroy() removes listeners.\n","workspace-menu/example.js":"const root = document.querySelector('.menu-demo');\nconst menu = MatteMenu.mount(root, {\n  onSelect(values) {\n    // Connect these values to your application.\n  }\n});\n// menu.reset() restores defaults; menu.destroy() removes listeners.\n","status-menu/example.js":"const root = document.querySelector('.menu-demo');\nconst menu = MatteMenu.mount(root, {\n  onSelect(values) {\n    // Connect these values to your application.\n  }\n});\n// menu.reset() restores defaults; menu.destroy() removes listeners.\n","assignee-menu/example.js":"const root = document.querySelector('.menu-demo');\nconst menu = MatteMenu.mount(root, {\n  onSelect(values) {\n    // Connect these values to your application.\n  }\n});\n// menu.reset() restores defaults; menu.destroy() removes listeners.\n","appearance-menu/example.js":"const root = document.querySelector('.menu-demo');\nconst menu = MatteMenu.mount(root, {\n  onSelect(values) {\n    // Connect these values to your application.\n  }\n});\n// menu.reset() restores defaults; menu.destroy() removes listeners.\n","density-menu/example.js":"const root = document.querySelector('.menu-demo');\nconst menu = MatteMenu.mount(root, {\n  onSelect(values) {\n    // Connect these values to your application.\n  }\n});\n// menu.reset() restores defaults; menu.destroy() removes listeners.\n","export-menu/example.js":"const root = document.querySelector('.menu-demo');\nconst menu = MatteMenu.mount(root, {\n  onSelect(values) {\n    // Connect these values to your application.\n  }\n});\n// menu.reset() restores defaults; menu.destroy() removes listeners.\n","permissions-menu/example.js":"const root = document.querySelector('.menu-demo');\nconst menu = MatteMenu.mount(root, {\n  onSelect(values) {\n    // Connect these values to your application.\n  }\n});\n// menu.reset() restores defaults; menu.destroy() removes listeners.\n","snooze-menu/example.js":"const root = document.querySelector('.menu-demo');\nconst menu = MatteMenu.mount(root, {\n  onSelect(values) {\n    // Connect these values to your application.\n  }\n});\n// menu.reset() restores defaults; menu.destroy() removes listeners.\n","move-menu/example.js":"const root = document.querySelector('.menu-demo');\nconst menu = MatteMenu.mount(root, {\n  onSelect(values) {\n    // Connect these values to your application.\n  }\n});\n// menu.reset() restores defaults; menu.destroy() removes listeners.\n","insert-menu/example.js":"const root = document.querySelector('.menu-demo');\nconst menu = MatteMenu.mount(root, {\n  onSelect(values) {\n    // Connect these values to your application.\n  }\n});\n// menu.reset() restores defaults; menu.destroy() removes listeners.\n","tabs-navigation/example.js":"const navigation = MatteNavigation.mount(document.querySelector('.nav-demo'));\n// Connect the local handlers in buttons.js to your own router or content.\nwindow.SLNavigationInstance = navigation;\nwindow.addEventListener('pagehide', e => { if (!e.persisted) navigation.destroy(); });\n","views-navigation/example.js":"const navigation = MatteNavigation.mount(document.querySelector('.nav-demo'));\n// Connect the local handlers in buttons.js to your own router or content.\nwindow.SLNavigationInstance = navigation;\nwindow.addEventListener('pagehide', e => { if (!e.persisted) navigation.destroy(); });\n","breadcrumbs-navigation/example.js":"const navigation = MatteNavigation.mount(document.querySelector('.nav-demo'));\n// Connect the local handlers in buttons.js to your own router or content.\nwindow.SLNavigationInstance = navigation;\nwindow.addEventListener('pagehide', e => { if (!e.persisted) navigation.destroy(); });\n","pages-navigation/example.js":"const navigation = MatteNavigation.mount(document.querySelector('.nav-demo'));\n// Connect the local handlers in buttons.js to your own router or content.\nwindow.SLNavigationInstance = navigation;\nwindow.addEventListener('pagehide', e => { if (!e.persisted) navigation.destroy(); });\n","sidebar-navigation/example.js":"const navigation = MatteNavigation.mount(document.querySelector('.nav-demo'));\n// Connect the local handlers in buttons.js to your own router or content.\nwindow.SLNavigationInstance = navigation;\nwindow.addEventListener('pagehide', e => { if (!e.persisted) navigation.destroy(); });\n","steps-navigation/example.js":"const navigation = MatteNavigation.mount(document.querySelector('.nav-demo'));\n// Connect the local handlers in buttons.js to your own router or content.\nwindow.SLNavigationInstance = navigation;\nwindow.addEventListener('pagehide', e => { if (!e.persisted) navigation.destroy(); });\n","dock-navigation/example.js":"const navigation = MatteNavigation.mount(document.querySelector('.nav-demo'));\n// Connect the local handlers in buttons.js to your own router or content.\nwindow.SLNavigationInstance = navigation;\nwindow.addEventListener('pagehide', e => { if (!e.persisted) navigation.destroy(); });\n","sections-navigation/example.js":"const navigation = MatteNavigation.mount(document.querySelector('.nav-demo'));\n// Connect the local handlers in buttons.js to your own router or content.\nwindow.SLNavigationInstance = navigation;\nwindow.addEventListener('pagehide', e => { if (!e.persisted) navigation.destroy(); });\n","carousel-navigation/example.js":"const navigation = MatteNavigation.mount(document.querySelector('.nav-demo'));\n// Connect the local handlers in buttons.js to your own router or content.\nwindow.SLNavigationInstance = navigation;\nwindow.addEventListener('pagehide', e => { if (!e.persisted) navigation.destroy(); });\n","tree-navigation/example.js":"const navigation = MatteNavigation.mount(document.querySelector('.nav-demo'));\n// Connect the local handlers in buttons.js to your own router or content.\nwindow.SLNavigationInstance = navigation;\nwindow.addEventListener('pagehide', e => { if (!e.persisted) navigation.destroy(); });\n","modal-overlay/example.js":"import { overlay } from './buttons.js';\nconst root = document.querySelector('.overlay-demo');\nroot.addEventListener('overlaycommit', event => {\n  // Connect event.detail to your application. This preview only updates local UI.\n});\nroot.addEventListener('overlayaction', event => { /* event.detail.value */ });\nroot.addEventListener('overlaytourcomplete', () => { /* Local tour finished. */ });\n// overlay.open(); overlay.close(); overlay.reset(); overlay.destroy();\n","drawer-overlay/example.js":"import { overlay } from './buttons.js';\nconst root = document.querySelector('.overlay-demo');\nroot.addEventListener('overlaycommit', event => {\n  // Connect event.detail to your application. This preview only updates local UI.\n});\nroot.addEventListener('overlayaction', event => { /* event.detail.value */ });\nroot.addEventListener('overlaytourcomplete', () => { /* Local tour finished. */ });\n// overlay.open(); overlay.close(); overlay.reset(); overlay.destroy();\n","sheet-overlay/example.js":"import { overlay } from './buttons.js';\nconst root = document.querySelector('.overlay-demo');\nroot.addEventListener('overlaycommit', event => {\n  // Connect event.detail to your application. This preview only updates local UI.\n});\nroot.addEventListener('overlayaction', event => { /* event.detail.value */ });\nroot.addEventListener('overlaytourcomplete', () => { /* Local tour finished. */ });\n// overlay.open(); overlay.close(); overlay.reset(); overlay.destroy();\n","popover-overlay/example.js":"import { overlay } from './buttons.js';\nconst root = document.querySelector('.overlay-demo');\nroot.addEventListener('overlaycommit', event => {\n  // Connect event.detail to your application. This preview only updates local UI.\n});\nroot.addEventListener('overlayaction', event => { /* event.detail.value */ });\nroot.addEventListener('overlaytourcomplete', () => { /* Local tour finished. */ });\n// overlay.open(); overlay.close(); overlay.reset(); overlay.destroy();\n","tooltip-overlay/example.js":"import { overlay } from './buttons.js';\nconst root = document.querySelector('.overlay-demo');\nroot.addEventListener('overlaycommit', event => {\n  // Connect event.detail to your application. This preview only updates local UI.\n});\nroot.addEventListener('overlayaction', event => { /* event.detail.value */ });\nroot.addEventListener('overlaytourcomplete', () => { /* Local tour finished. */ });\n// overlay.open(); overlay.close(); overlay.reset(); overlay.destroy();\n","confirm-overlay/example.js":"import { overlay } from './buttons.js';\nconst root = document.querySelector('.overlay-demo');\nroot.addEventListener('overlaycommit', event => {\n  // Connect event.detail to your application. This preview only updates local UI.\n});\nroot.addEventListener('overlayaction', event => { /* event.detail.value */ });\nroot.addEventListener('overlaytourcomplete', () => { /* Local tour finished. */ });\n// overlay.open(); overlay.close(); overlay.reset(); overlay.destroy();\n","palette-overlay/example.js":"import { overlay } from './buttons.js';\nconst root = document.querySelector('.overlay-demo');\nroot.addEventListener('overlaycommit', event => {\n  // Connect event.detail to your application. This preview only updates local UI.\n});\nroot.addEventListener('overlayaction', event => { /* event.detail.value */ });\nroot.addEventListener('overlaytourcomplete', () => { /* Local tour finished. */ });\n// overlay.open(); overlay.close(); overlay.reset(); overlay.destroy();\n","document-overlay/example.js":"import { overlay } from './buttons.js';\nconst root = document.querySelector('.overlay-demo');\nroot.addEventListener('overlaycommit', event => {\n  // Connect event.detail to your application. This preview only updates local UI.\n});\nroot.addEventListener('overlayaction', event => { /* event.detail.value */ });\nroot.addEventListener('overlaytourcomplete', () => { /* Local tour finished. */ });\n// overlay.open(); overlay.close(); overlay.reset(); overlay.destroy();\n","quickedit-overlay/example.js":"import { overlay } from './buttons.js';\nconst root = document.querySelector('.overlay-demo');\nroot.addEventListener('overlaycommit', event => {\n  // Connect event.detail to your application. This preview only updates local UI.\n});\nroot.addEventListener('overlayaction', event => { /* event.detail.value */ });\nroot.addEventListener('overlaytourcomplete', () => { /* Local tour finished. */ });\n// overlay.open(); overlay.close(); overlay.reset(); overlay.destroy();\n","tour-overlay/example.js":"import { overlay } from './buttons.js';\nconst root = document.querySelector('.overlay-demo');\nroot.addEventListener('overlaycommit', event => {\n  // Connect event.detail to your application. This preview only updates local UI.\n});\nroot.addEventListener('overlayaction', event => { /* event.detail.value */ });\nroot.addEventListener('overlaytourcomplete', () => { /* Local tour finished. */ });\n// overlay.open(); overlay.close(); overlay.reset(); overlay.destroy();\n","toast-feedback/example.js":"// Interactive local preview. No real requests or data changes.\nconst root = document.querySelector('.fb-demo');\nconst preview = MatteFeedbackPreview.mount(root, MatteFeedback.mount);\n\n// Application integration: replace the preview mount above with:\n// const feedback = MatteFeedback.mount(root, {\n//   onAction: async (action, { signal, onProgress }) => {\n//     // Await your real operation; reject on failure, honor signal.\n//     // For measurable work, call onProgress(percent) with 0–100.\n//   }\n// });\n// Remove the [data-demo] controls when connecting your app.\n// feedback.show({title, detail, tone}) presents a literal-text message.\n// feedback.push({title, detail}) queues a notification (stack only).\n// feedback.update(percent), feedback.setMode('unknown') control progress.\n// feedback.run(action) calls onAction; feedback.cancel() aborts it.\n// feedback.reset() restores defaults; feedback.destroy() removes listeners.\n// Success is shown only after onAction resolves. Demo success is not a server response.\n","undo-feedback/example.js":"// Interactive local preview. No real requests or data changes.\nconst root = document.querySelector('.fb-demo');\nconst preview = MatteFeedbackPreview.mount(root, MatteFeedback.mount);\n\n// Application integration: replace the preview mount above with:\n// const feedback = MatteFeedback.mount(root, {\n//   onAction: async (action, { signal, onProgress }) => {\n//     // Await your real operation; reject on failure, honor signal.\n//     // For measurable work, call onProgress(percent) with 0–100.\n//   }\n// });\n// Remove the [data-demo] controls when connecting your app.\n// feedback.show({title, detail, tone}) presents a literal-text message.\n// feedback.push({title, detail}) queues a notification (stack only).\n// feedback.update(percent), feedback.setMode('unknown') control progress.\n// feedback.run(action) calls onAction; feedback.cancel() aborts it.\n// feedback.reset() restores defaults; feedback.destroy() removes listeners.\n// Success is shown only after onAction resolves. Demo success is not a server response.\n","stack-feedback/example.js":"// Interactive local preview. No real requests or data changes.\nconst root = document.querySelector('.fb-demo');\nconst preview = MatteFeedbackPreview.mount(root, MatteFeedback.mount);\n\n// Application integration: replace the preview mount above with:\n// const feedback = MatteFeedback.mount(root, {\n//   onAction: async (action, { signal, onProgress }) => {\n//     // Await your real operation; reject on failure, honor signal.\n//     // For measurable work, call onProgress(percent) with 0–100.\n//   }\n// });\n// Remove the [data-demo] controls when connecting your app.\n// feedback.show({title, detail, tone}) presents a literal-text message.\n// feedback.push({title, detail}) queues a notification (stack only).\n// feedback.update(percent), feedback.setMode('unknown') control progress.\n// feedback.run(action) calls onAction; feedback.cancel() aborts it.\n// feedback.reset() restores defaults; feedback.destroy() removes listeners.\n// Success is shown only after onAction resolves. Demo success is not a server response.\n","alert-feedback/example.js":"// Interactive local preview. No real requests or data changes.\nconst root = document.querySelector('.fb-demo');\nconst preview = MatteFeedbackPreview.mount(root, MatteFeedback.mount);\n\n// Application integration: replace the preview mount above with:\n// const feedback = MatteFeedback.mount(root, {\n//   onAction: async (action, { signal, onProgress }) => {\n//     // Await your real operation; reject on failure, honor signal.\n//     // For measurable work, call onProgress(percent) with 0–100.\n//   }\n// });\n// Remove the [data-demo] controls when connecting your app.\n// feedback.show({title, detail, tone}) presents a literal-text message.\n// feedback.push({title, detail}) queues a notification (stack only).\n// feedback.update(percent), feedback.setMode('unknown') control progress.\n// feedback.run(action) calls onAction; feedback.cancel() aborts it.\n// feedback.reset() restores defaults; feedback.destroy() removes listeners.\n// Success is shown only after onAction resolves. Demo success is not a server response.\n","connection-feedback/example.js":"// Interactive local preview. No real requests or data changes.\nconst root = document.querySelector('.fb-demo');\nconst preview = MatteFeedbackPreview.mount(root, MatteFeedback.mount);\n\n// Application integration: replace the preview mount above with:\n// const feedback = MatteFeedback.mount(root, {\n//   onAction: async (action, { signal, onProgress }) => {\n//     // Await your real operation; reject on failure, honor signal.\n//     // For measurable work, call onProgress(percent) with 0–100.\n//   }\n// });\n// Remove the [data-demo] controls when connecting your app.\n// feedback.show({title, detail, tone}) presents a literal-text message.\n// feedback.push({title, detail}) queues a notification (stack only).\n// feedback.update(percent), feedback.setMode('unknown') control progress.\n// feedback.run(action) calls onAction; feedback.cancel() aborts it.\n// feedback.reset() restores defaults; feedback.destroy() removes listeners.\n// Success is shown only after onAction resolves. Demo success is not a server response.\n","linear-feedback/example.js":"// Interactive local preview. No real requests or data changes.\nconst root = document.querySelector('.fb-demo');\nconst preview = MatteFeedbackPreview.mount(root, MatteFeedback.mount);\n\n// Application integration: replace the preview mount above with:\n// const feedback = MatteFeedback.mount(root, {\n//   onAction: async (action, { signal, onProgress }) => {\n//     // Await your real operation; reject on failure, honor signal.\n//     // For measurable work, call onProgress(percent) with 0–100.\n//   }\n// });\n// Remove the [data-demo] controls when connecting your app.\n// feedback.show({title, detail, tone}) presents a literal-text message.\n// feedback.push({title, detail}) queues a notification (stack only).\n// feedback.update(percent), feedback.setMode('unknown') control progress.\n// feedback.run(action) calls onAction; feedback.cancel() aborts it.\n// feedback.reset() restores defaults; feedback.destroy() removes listeners.\n// Success is shown only after onAction resolves. Demo success is not a server response.\n","circular-feedback/example.js":"// Interactive local preview. No real requests or data changes.\nconst root = document.querySelector('.fb-demo');\nconst preview = MatteFeedbackPreview.mount(root, MatteFeedback.mount);\n\n// Application integration: replace the preview mount above with:\n// const feedback = MatteFeedback.mount(root, {\n//   onAction: async (action, { signal, onProgress }) => {\n//     // Await your real operation; reject on failure, honor signal.\n//     // For measurable work, call onProgress(percent) with 0–100.\n//   }\n// });\n// Remove the [data-demo] controls when connecting your app.\n// feedback.show({title, detail, tone}) presents a literal-text message.\n// feedback.push({title, detail}) queues a notification (stack only).\n// feedback.update(percent), feedback.setMode('unknown') control progress.\n// feedback.run(action) calls onAction; feedback.cancel() aborts it.\n// feedback.reset() restores defaults; feedback.destroy() removes listeners.\n// Success is shown only after onAction resolves. Demo success is not a server response.\n","skeleton-feedback/example.js":"// Interactive local preview. No real requests or data changes.\nconst root = document.querySelector('.fb-demo');\nconst preview = MatteFeedbackPreview.mount(root, MatteFeedback.mount);\n\n// Application integration: replace the preview mount above with:\n// const feedback = MatteFeedback.mount(root, {\n//   onAction: async (action, { signal, onProgress }) => {\n//     // Await your real operation; reject on failure, honor signal.\n//     // For measurable work, call onProgress(percent) with 0–100.\n//   }\n// });\n// Remove the [data-demo] controls when connecting your app.\n// feedback.show({title, detail, tone}) presents a literal-text message.\n// feedback.push({title, detail}) queues a notification (stack only).\n// feedback.update(percent), feedback.setMode('unknown') control progress.\n// feedback.run(action) calls onAction; feedback.cancel() aborts it.\n// feedback.reset() restores defaults; feedback.destroy() removes listeners.\n// Success is shown only after onAction resolves. Demo success is not a server response.\n","loading-feedback/example.js":"// Interactive local preview. No real requests or data changes.\nconst root = document.querySelector('.fb-demo');\nconst preview = MatteFeedbackPreview.mount(root, MatteFeedback.mount);\n\n// Application integration: replace the preview mount above with:\n// const feedback = MatteFeedback.mount(root, {\n//   onAction: async (action, { signal, onProgress }) => {\n//     // Await your real operation; reject on failure, honor signal.\n//     // For measurable work, call onProgress(percent) with 0–100.\n//   }\n// });\n// Remove the [data-demo] controls when connecting your app.\n// feedback.show({title, detail, tone}) presents a literal-text message.\n// feedback.push({title, detail}) queues a notification (stack only).\n// feedback.update(percent), feedback.setMode('unknown') control progress.\n// feedback.run(action) calls onAction; feedback.cancel() aborts it.\n// feedback.reset() restores defaults; feedback.destroy() removes listeners.\n// Success is shown only after onAction resolves. Demo success is not a server response.\n","retry-feedback/example.js":"// Interactive local preview. No real requests or data changes.\nconst root = document.querySelector('.fb-demo');\nconst preview = MatteFeedbackPreview.mount(root, MatteFeedback.mount);\n\n// Application integration: replace the preview mount above with:\n// const feedback = MatteFeedback.mount(root, {\n//   onAction: async (action, { signal, onProgress }) => {\n//     // Await your real operation; reject on failure, honor signal.\n//     // For measurable work, call onProgress(percent) with 0–100.\n//   }\n// });\n// Remove the [data-demo] controls when connecting your app.\n// feedback.show({title, detail, tone}) presents a literal-text message.\n// feedback.push({title, detail}) queues a notification (stack only).\n// feedback.update(percent), feedback.setMode('unknown') control progress.\n// feedback.run(action) calls onAction; feedback.cancel() aborts it.\n// feedback.reset() restores defaults; feedback.destroy() removes listeners.\n// Success is shown only after onAction resolves. Demo success is not a server response.\n","metric-display/example.js":"// Local sample data. No network requests, uploads or account changes.\nconst root = document.querySelector('.dd-demo');\nconst display = MatteDataDisplay.mount(root, {\n  onChange(detail) {\n    // Connect selection to your application here.\n    // detail.kind, detail.action and state fields describe the change.\n  }\n});\n\n// Optional programmatic interaction:\n// display.setPeriod('month'); // week, month, quarter\n\n// display.state returns a read-only snapshot.\n// display.reset() restores the example; display.destroy() removes listeners.\n// Replace sample records in index.html and the datasets in buttons.js.\n// Change --dd-* variables in buttons.css to match your palette.\n// Preserve table headers, calendar grid roles, labels and keyboard handling.\n","table-display/example.js":"// Local sample data. No network requests, uploads or account changes.\nconst root = document.querySelector('.dd-demo');\nconst display = MatteDataDisplay.mount(root, {\n  onChange(detail) {\n    // Connect selection to your application here.\n    // detail.kind, detail.action and state fields describe the change.\n  }\n});\n\n// Optional programmatic interaction:\n// display.sort('size'); // name or size; repeat to reverse\n// display.select('brief'); // brief, icons, notes, tokens\n\n// display.state returns a read-only snapshot.\n// display.reset() restores the example; display.destroy() removes listeners.\n// Replace sample records in index.html and the datasets in buttons.js.\n// Change --dd-* variables in buttons.css to match your palette.\n// Preserve table headers, calendar grid roles, labels and keyboard handling.\n","avatars-display/example.js":"// Local sample data. No network requests, uploads or account changes.\nconst root = document.querySelector('.dd-demo');\nconst display = MatteDataDisplay.mount(root, {\n  onChange(detail) {\n    // Connect selection to your application here.\n    // detail.kind, detail.action and state fields describe the change.\n  }\n});\n\n// Optional programmatic interaction:\n// display.select('JC'); // AL, JC, MR, SK\n// display.toggle(true); // show additional members\n\n// display.state returns a read-only snapshot.\n// display.reset() restores the example; display.destroy() removes listeners.\n// Replace sample records in index.html and the datasets in buttons.js.\n// Change --dd-* variables in buttons.css to match your palette.\n// Preserve table headers, calendar grid roles, labels and keyboard handling.\n","status-display/example.js":"// Local sample data. No network requests, uploads or account changes.\nconst root = document.querySelector('.dd-demo');\nconst display = MatteDataDisplay.mount(root, {\n  onChange(detail) {\n    // Connect selection to your application here.\n    // detail.kind, detail.action and state fields describe the change.\n  }\n});\n\n// Optional programmatic interaction:\n// display.filter('active'); // all, active, paused\n\n// display.state returns a read-only snapshot.\n// display.reset() restores the example; display.destroy() removes listeners.\n// Replace sample records in index.html and the datasets in buttons.js.\n// Change --dd-* variables in buttons.css to match your palette.\n// Preserve table headers, calendar grid roles, labels and keyboard handling.\n","details-display/example.js":"// Local sample data. No network requests, uploads or account changes.\nconst root = document.querySelector('.dd-demo');\nconst display = MatteDataDisplay.mount(root, {\n  onChange(detail) {\n    // Connect selection to your application here.\n    // detail.kind, detail.action and state fields describe the change.\n  }\n});\n\n// Optional programmatic interaction:\n// display.toggle(true);\n\n// display.state returns a read-only snapshot.\n// display.reset() restores the example; display.destroy() removes listeners.\n// Replace sample records in index.html and the datasets in buttons.js.\n// Change --dd-* variables in buttons.css to match your palette.\n// Preserve table headers, calendar grid roles, labels and keyboard handling.\n","timeline-display/example.js":"// Local sample data. No network requests, uploads or account changes.\nconst root = document.querySelector('.dd-demo');\nconst display = MatteDataDisplay.mount(root, {\n  onChange(detail) {\n    // Connect selection to your application here.\n    // detail.kind, detail.action and state fields describe the change.\n  }\n});\n\n// Optional programmatic interaction:\n// display.toggle(true);\n\n// display.state returns a read-only snapshot.\n// display.reset() restores the example; display.destroy() removes listeners.\n// Replace sample records in index.html and the datasets in buttons.js.\n// Change --dd-* variables in buttons.css to match your palette.\n// Preserve table headers, calendar grid roles, labels and keyboard handling.\n","file-display/example.js":"// Local sample data. No network requests, uploads or account changes.\nconst root = document.querySelector('.dd-demo');\nconst display = MatteDataDisplay.mount(root, {\n  onChange(detail) {\n    // Connect selection to your application here.\n    // detail.kind, detail.action and state fields describe the change.\n  }\n});\n\n// Optional programmatic interaction:\n// display.select(1); // zero-based sample file index: 0, 1, 2\n\n// display.state returns a read-only snapshot.\n// display.reset() restores the example; display.destroy() removes listeners.\n// Replace sample records in index.html and the datasets in buttons.js.\n// Change --dd-* variables in buttons.css to match your palette.\n// Preserve table headers, calendar grid roles, labels and keyboard handling.\n","bar-display/example.js":"// Local sample data. No network requests, uploads or account changes.\nconst root = document.querySelector('.dd-demo');\nconst display = MatteDataDisplay.mount(root, {\n  onChange(detail) {\n    // Connect selection to your application here.\n    // detail.kind, detail.action and state fields describe the change.\n  }\n});\n\n// Optional programmatic interaction:\n// display.setPeriod('previous'); // current or previous\n// display.select(2); // weekday index: Monday = 0\n\n// display.state returns a read-only snapshot.\n// display.reset() restores the example; display.destroy() removes listeners.\n// Replace sample records in index.html and the datasets in buttons.js.\n// Change --dd-* variables in buttons.css to match your palette.\n// Preserve table headers, calendar grid roles, labels and keyboard handling.\n","donut-display/example.js":"// Local sample data. No network requests, uploads or account changes.\nconst root = document.querySelector('.dd-demo');\nconst display = MatteDataDisplay.mount(root, {\n  onChange(detail) {\n    // Connect selection to your application here.\n    // detail.kind, detail.action and state fields describe the change.\n  }\n});\n\n// Optional programmatic interaction:\n// display.select(1); // Assets = 0, Documents = 1, Exports = 2, Free = 3\n// display.select(null); // return to total usage\n\n// display.state returns a read-only snapshot.\n// display.reset() restores the example; display.destroy() removes listeners.\n// Replace sample records in index.html and the datasets in buttons.js.\n// Change --dd-* variables in buttons.css to match your palette.\n// Preserve table headers, calendar grid roles, labels and keyboard handling.\n","calendar-display/example.js":"// Local sample data. No network requests, uploads or account changes.\nconst root = document.querySelector('.dd-demo');\nconst display = MatteDataDisplay.mount(root, {\n  onChange(detail) {\n    // Connect selection to your application here.\n    // detail.kind, detail.action and state fields describe the change.\n  }\n});\n\n// Optional programmatic interaction:\n// display.select('2026-09-18'); // ISO calendar date\n// display.showMonth(2026, 10); // month is 1–12\n\n// display.state returns a read-only snapshot.\n// display.reset() restores the example; display.destroy() removes listeners.\n// Replace sample records in index.html and the datasets in buttons.js.\n// Change --dd-* variables in buttons.css to match your palette.\n// Preserve table headers, calendar grid roles, labels and keyboard handling.\n","buttons-split-action-v11/example.js":"// Explicit local demonstration. No network or device operation is performed.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application integration:\n// Pass onExport({ format, signal }); resolve only after export creation or handoff succeeds. Reject on failure. The preview creates no file.\n// start() returns Promise<boolean>; open(), close(), cancel(), reset(), destroy(). state is { phase, format, open }. sl:action reports format selection and action outcomes.\n// Replace mountPreview above with SLComponent.mount(root, options).\n// Call controller.destroy() before removing or replacing the component.\n// reset() restores the control only; it cannot undo completed external work.\n","buttons-cancellable-process-v11/example.js":"// Explicit local demonstration. No network or device operation is performed.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application integration:\n// Pass onProcess({ signal, onProgress }). Report finite fractions from 0 to 1. Progress is clamped, monotonic within one request and ignores stale reports after cancellation. The preview simulates progress.\n// start() returns Promise<boolean>; cancel(), reset(), destroy(). state is { phase, progress } where progress is 0–100. sl:action reports completion, failure or cancellation.\n// Replace mountPreview above with SLComponent.mount(root, options).\n// Call controller.destroy() before removing or replacing the component.\n// reset() restores the control only; it cannot undo completed external work.\n","buttons-hold-confirm-v11/example.js":"// Explicit local demonstration. No network or device operation is performed.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application integration:\n// Pass onConfirm({ signal }); resolve only after the archive succeeds. holdMs defaults to 1200 and accepts 500–5000 ms. Releasing early or leaving the button cancels the hold. Keyboard activation opens an untimed confirmation.\n// confirm() returns Promise<boolean> and bypasses the UI confirmation for trusted application code; open(), close(), cancel(), reset(), destroy(). state is { phase, open, holding }. sl:action reports operation outcomes.\n// Replace mountPreview above with SLComponent.mount(root, options).\n// Call controller.destroy() before removing or replacing the component.\n// reset() restores the control only; it cannot undo completed external work.\n","buttons-action-queue-v11/example.js":"// Explicit local demonstration. No network or device operation is performed.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application integration:\n// Pass onRun({ id, label, signal }) for each job. Jobs run serially; successful jobs leave the queue. Cancellation or failure retains the current and waiting jobs for retry. Additions during processing are appended. Preview jobs are simulated.\n// add(label?) and remove(zeroBasedIndex) return booleans. Labels are literal text. start() returns Promise<boolean>; cancel(), reset(), destroy(). state contains phase, a copied items array, completed and activeId. The queue holds at most three jobs.\n// Replace mountPreview above with SLComponent.mount(root, options).\n// Call controller.destroy() before removing or replacing the component.\n// reset() restores the control only; it cannot undo completed external work.\n","buttons-record-take-v11/example.js":"// Explicit local demonstration. No network or device operation is performed.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application integration:\n// Pass onStart({ signal }) returning a session object with an idempotent synchronous dispose(), and onStop({ session, signal }). dispose() must stop all device tracks. The start signal stays active until session cleanup. A late session is disposed if start was canceled. Resolve onStop only when stopping succeeds.\n// start() and stop() return Promise<boolean>; cancel() aborts only the pending request, so a canceled stop leaves the session recording. reset() and destroy() always dispose the session and abort its lifetime signal. state is { phase, recording, elapsed } in seconds. sl:action reports outcomes; the preview never accesses a microphone.\n// Replace mountPreview above with SLComponent.mount(root, options).\n// Call controller.destroy() before removing or replacing the component.\n// reset() restores the control only; it cannot undo completed external work.\n","buttons-review-decision-v11/example.js":"// Explicit local demonstration. No network or device operation is performed.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application integration:\n// This component selects a local decision only. Listen for sl:action and read event.detail.decision (approve, reject or null) to connect the next workflow step. setDecision(value) silently restores application state.\n// choose(value), undo(), setDecision(value) return booleans; reset(), destroy(). state is { phase, decision }. Approve and Reject are native buttons with aria-pressed. Undo restores one previous decision.\n// Replace mountPreview above with SLComponent.mount(root, options).\n// Call controller.destroy() before removing or replacing the component.\n// reset() restores the control only; it cannot undo completed external work.\n","buttons-refresh-snapshot-v11/example.js":"// Explicit local demonstration. No network or device operation is performed.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application integration:\n// Pass onRefresh({ count, version, signal }) returning { count, label? }. count must be a non-negative safe integer; label is literal status text. initialCount defaults to 24. A failed or canceled refresh retains the previous snapshot.\n// start() returns Promise<boolean>; cancel(), reset(), destroy(). state is { phase, count, version, updatedAt }; updatedAt is a local success timestamp in milliseconds or null. sl:action reports outcomes.\n// Replace mountPreview above with SLComponent.mount(root, options).\n// Call controller.destroy() before removing or replacing the component.\n// reset() restores the control only; it cannot undo completed external work.\n","buttons-duplicate-item-v11/example.js":"// Explicit local demonstration. No file, clipboard or network operation occurs.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application integration: replace the line above with\n// const controller = SLComponent.mount(root, {\n//   item: { id: 'note-1', label: 'Project brief' },\n//   onDuplicate: ({ source, signal }) => duplicateInYourApp(source.id, { signal })\n// });\n// Resolve onDuplicate only after the copy exists. Return { id, label } with a new\n// id; reject on failure. id and label are non-empty strings up to 160 characters.\n// copyNumber is a local count, not a globally unique identifier for your backend.\n// Labels render as literal text. sl:action reports confirmed results and failures.\n// start() returns Promise<boolean>; cancel() returns boolean. Escape cancels.\n// reset() clears this preview state, not copies already created in your app.\n// destroy() aborts pending work, removes listeners and ignores late results.\n// Cancellation is best-effort: the application owns rollback/reconciliation if\n// its operation completed despite abort. Remounting disposes the old controller.\n","buttons-repeat-stepper-v11/example.js":"// Explicit local demonstration. No network or device operation is performed.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application integration:\n// Pass integer min, max and value. Defaults are 1, 12 and 3; range limits are 0–999. Listen for sl:action and read event.detail.value. Holding begins repetition after 400 ms and stops on release, leaving the button, blur or a boundary.\n// setValue(integer, notify = false) clamps to the range and returns whether the value changed. reset(), destroy(). state is { phase, value, min, max }. Native keyboard clicks change once; arrows change by one, Home/End reach limits, Escape stops repeat.\n// Replace mountPreview above with SLComponent.mount(root, options).\n// Call controller.destroy() before removing or replacing the component.\n// reset() restores the control only; it cannot undo completed external work.\n","buttons-shuffle-choice-v11/example.js":"// Explicit local demonstration. No network or device operation is performed.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application integration:\n// Pass 2–20 distinct non-empty choices. All values render as literal text, including Unicode. Optional random() must return a finite number in [0, 1). The algorithm always picks a different index, then offers one-level undo.\n// shuffle() and undo() return booleans; reset(), destroy(). state is { phase, index, choice, canUndo }. sl:action reports index and choice. No requests or content generation occur.\n// Replace mountPreview above with SLComponent.mount(root, options).\n// Call controller.destroy() before removing or replacing the component.\n// reset() restores the control only; it cannot undo completed external work.\n","inputs-tokens-v11/example.js":"// This example changes local field state only. It makes no external request.\n// In application code: const field = SLComponent.mount(root, { onChange(state) {} });\n// Read field.state, call field.reset(), and call field.destroy() before removing the root.\n// options.value is an array of up to six unique strings (1–40 characters each). state = { value: string[], draft: string }. Enter commits one whole tag; remove buttons delete a tag and return focus to the input.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n","inputs-combobox-v11/example.js":"// This example changes local field state only. It makes no external request.\n// In application code: const field = SLComponent.mount(root, { onChange(state) {} });\n// Read field.state, call field.reset(), and call field.destroy() before removing the root.\n// options.items = [{ id, label, disabled? }]; options.value is an available ID or null. state = { value: ID|null, label, query, open }. Arrow keys skip disabled items, Enter commits, Escape and blur restore the selected label. Free text never becomes a selected ID.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n","inputs-date-range-v11/example.js":"// This example changes local field state only. It makes no external request.\n// In application code: const field = SLComponent.mount(root, { onChange(state) {} });\n// Read field.state, call field.reset(), and call field.destroy() before removing the root.\n// options.value = { start: YYYY-MM-DD, end: YYYY-MM-DD }, ordered and within 2000–2100. state = { value: { start, end }, valid, days: number|null }. Inclusive day arithmetic uses UTC calendar days and is independent of daylight-saving transitions. The project calendar uses the root language (English by default), a Monday-first keyboard grid and previous/next month navigation. Arrow keys move by day/week; Home/End move within a week; PageUp/PageDown change month (Shift changes year). ISO text entry remains available. Escape closes without selecting.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n","inputs-time-v11/example.js":"// This example changes local field state only. It makes no external request.\n// In application code: const field = SLComponent.mount(root, { onChange(state) {} });\n// Read field.state, call field.reset(), and call field.destroy() before removing the root.\n// options.value is HH:MM from 00:00 to 23:55 in five-minute steps. state = { value, valid, minutes: number|null }. Presets update the local field; this component does not schedule notifications or convert time zones.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n","inputs-duration-v11/example.js":"// This example changes local field state only. It makes no external request.\n// In application code: const field = SLComponent.mount(root, { onChange(state) {} });\n// Read field.state, call field.reset(), and call field.destroy() before removing the root.\n// options.value is integer total minutes (0–1439). state = { value: number|null, valid, hours: string, minutes: string }. Quarter-hour actions clamp to the boundaries and carry between units; invalid drafts stay editable and disable adjustments.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n","inputs-measurement-v11/example.js":"// This example changes local field state only. It makes no external request.\n// In application code: const field = SLComponent.mount(root, { onChange(state) {} });\n// Read field.state, call field.reset(), and call field.destroy() before removing the root.\n// options.value is millimetres (0–10000); options.unit is cm or in. state = { value: millimetres|null, unit, draft, valid }. Unit toggles retain the canonical millimetres; display rounds to eight decimals. Editing updates the canonical value. Invalid drafts disable the unit switch.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n","inputs-editable-title-v11/example.js":"// This example changes local field state only. It makes no external request.\n// In application code: const field = SLComponent.mount(root, { onChange(state) {} });\n// Read field.state, call field.reset(), and call field.destroy() before removing the root.\n// options.value is a nonblank string of at most 80 characters. state = { value, draft, editing }. Apply commits the exact text locally and emits sl:change; Cancel/Escape discard the draft. Enter respects IME composition. No server persistence is implied.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n","inputs-key-value-v11/example.js":"// This example changes local field state only. It makes no external request.\n// In application code: const field = SLComponent.mount(root, { onChange(state) {} });\n// Read field.state, call field.reset(), and call field.destroy() before removing the root.\n// options.value = [{ key, value }] with up to three pairs; keys are unique, nonblank and at most 32 characters; values are at most 80 characters. state = { value: pair[], valid }. An array preserves arbitrary Unicode keys (including __proto__) as literal data. Draft input is not rebuilt while typing.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n","inputs-color-v11/example.js":"// This example changes local field state only. It makes no external request.\n// In application code: const field = SLComponent.mount(root, { onChange(state) {} });\n// Read field.state, call field.reset(), and call field.destroy() before removing the root.\n// options.value is a six-digit #RRGGBB color. state = { value: lowercase hex|null, draft, valid }. Hex typing preserves case and caret. The project palette provides a pointer-controlled saturation/brightness plane and keyboard-accessible hue, saturation and brightness sliders. Apply commits the draft; Escape cancels. Presets apply immediately. The value is opaque sRGB; no alpha or color-space conversion is claimed.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n","inputs-attachment-v11/example.js":"// This example changes local field state only. It makes no external request.\n// In application code: const field = SLComponent.mount(root, { onChange(state) {} });\n// Read field.state, call field.reset(), and call field.destroy() before removing the root.\n// state = { file: File|null, value: { name, size, type, lastModified }|null, valid }. Validates the .pdf/.txt/.md extension and a 5 MiB byte limit only; this is not content validation. No file bytes are read. Pass state.file to your own operation only when state.valid is true. Normal mount uses the native file chooser. mountPreview uses built-in in-memory sample files and never opens the system chooser. options.demo=true selects that explicit demo mode; demo File objects are samples, not user documents.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n","selection-presence-rail-v11/example.js":"// Standalone demonstration: all selections remain local to this component.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// In your app, replace the call above with:\n// const controller = SLComponent.mount(root, { onChange(state) { /* Connect your setting here. */ } });\n// Use mode: available, focus or away. Arrow keys wrap between positions; Home and End select endpoints.\n// Initial state: { mode: \"available\" }\n// controller.setState(patch) validates and updates silently; pass { emit: true } to notify.\n// selectionchange bubbles across Shadow DOM; event.detail.state is an independent snapshot.\n// controller.state returns a copy. reset() restores the package defaults without an event.\n// destroy() removes listeners and stops CSS motion; repeated calls are safe.\n// Repeated mount on the same root returns its existing controller until destroyed.\n","selection-reading-density-v11/example.js":"// Standalone demonstration: all selections remain local to this component.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// In your app, replace the call above with:\n// const controller = SLComponent.mount(root, { onChange(state) { /* Connect your setting here. */ } });\n// Use density: compact, balanced or roomy. Previous and next buttons stop at the endpoints; the three sample lines keep their content.\n// Initial state: { density: \"balanced\" }\n// controller.setState(patch) validates and updates silently; pass { emit: true } to notify.\n// selectionchange bubbles across Shadow DOM; event.detail.state is an independent snapshot.\n// controller.state returns a copy. reset() restores the package defaults without an event.\n// destroy() removes listeners and stops CSS motion; repeated calls are safe.\n// Repeated mount on the same root returns its existing controller until destroyed.\n","selection-reading-aids-v11/example.js":"// Standalone demonstration: all selections remain local to this component.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// In your app, replace the call above with:\n// const controller = SLComponent.mount(root, { onChange(state) { /* Connect your setting here. */ } });\n// Boolean enabled gates guide and phrases without changing them. Compute effective settings as enabled && guide and enabled && phrases.\n// Initial state: { enabled: true, guide: true, phrases: false }\n// controller.setState(patch) validates and updates silently; pass { emit: true } to notify.\n// selectionchange bubbles across Shadow DOM; event.detail.state is an independent snapshot.\n// controller.state returns a copy. reset() restores the package defaults without an event.\n// destroy() removes listeners and stops CSS motion; repeated calls are safe.\n// Repeated mount on the same root returns its existing controller until destroyed.\n","selection-focus-override-v11/example.js":"// Standalone demonstration: all selections remain local to this component.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// In your app, replace the call above with:\n// const controller = SLComponent.mount(root, { onChange(state) { /* Connect your setting here. */ } });\n// Boolean focus temporarily overrides saved activity and hints to off. The state getter keeps the saved preferences; effective values are !focus && activity and !focus && hints.\n// Initial state: { focus: false, activity: true, hints: true }\n// controller.setState(patch) validates and updates silently; pass { emit: true } to notify.\n// selectionchange bubbles across Shadow DOM; event.detail.state is an independent snapshot.\n// controller.state returns a copy. reset() restores the package defaults without an event.\n// destroy() removes listeners and stops CSS motion; repeated calls are safe.\n// Repeated mount on the same root returns its existing controller until destroyed.\n","selection-snap-modes-v11/example.js":"// Standalone demonstration: all selections remain local to this component.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// In your app, replace the call above with:\n// const controller = SLComponent.mount(root, { onChange(state) { /* Connect your setting here. */ } });\n// Use grid and guides independently with off, soft or firm. Each row is its own radio group and tab stop; the illustration is a local guide sample.\n// Initial state: { grid: \"soft\", guides: \"firm\" }\n// controller.setState(patch) validates and updates silently; pass { emit: true } to notify.\n// selectionchange bubbles across Shadow DOM; event.detail.state is an independent snapshot.\n// controller.state returns a copy. reset() restores the package defaults without an event.\n// destroy() removes listeners and stops CSS motion; repeated calls are safe.\n// Repeated mount on the same root returns its existing controller until destroyed.\n","selection-week-rhythm-v11/example.js":"// Standalone demonstration: all selections remain local to this component.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// In your app, replace the call above with:\n// const controller = SLComponent.mount(root, { onChange(state) { /* Connect your setting here. */ } });\n// days contains unique keys mon through sun, normalized into week order. Day buttons use Space or Enter. Weekdays and Weekend replace the whole selection; Clear allows an empty selection.\n// Initial state: { days: [\"mon\", \"tue\", \"wed\", \"thu\", \"fri\"] }\n// controller.setState(patch) validates and updates silently; pass { emit: true } to notify.\n// selectionchange bubbles across Shadow DOM; event.detail.state is an independent snapshot.\n// controller.state returns a copy. reset() restores the package defaults without an event.\n// destroy() removes listeners and stops CSS motion; repeated calls are safe.\n// Repeated mount on the same root returns its existing controller until destroyed.\n","selection-measurement-units-v11/example.js":"// Standalone demonstration: all selections remain local to this component.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// In your app, replace the call above with:\n// const controller = SLComponent.mount(root, { onChange(state) { /* Connect your setting here. */ } });\n// units is metric or imperial. The sample starts from 12 km and 480 m, using 1 mi = 1.609344 km and 1 ft = 0.3048 m. Repeated switching never converts rounded output back into source values.\n// Initial state: { units: \"metric\" }\n// controller.setState(patch) validates and updates silently; pass { emit: true } to notify.\n// selectionchange bubbles across Shadow DOM; event.detail.state is an independent snapshot.\n// controller.state returns a copy. reset() restores the package defaults without an event.\n// destroy() removes listeners and stops CSS motion; repeated calls are safe.\n// Repeated mount on the same root returns its existing controller until destroyed.\n","selection-inherited-setting-v11/example.js":"// Standalone demonstration: all selections remain local to this component.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// In your app, replace the call above with:\n// const controller = SLComponent.mount(root, { onChange(state) { /* Connect your setting here. */ } });\n// source is workspace or custom. workspace and custom are booleans. Update the inherited value through setState({ workspace: value }); the custom choice survives source changes. Effective value is state[state.source].\n// Initial state: { source: \"workspace\", workspace: true, custom: false }\n// controller.setState(patch) validates and updates silently; pass { emit: true } to notify.\n// selectionchange bubbles across Shadow DOM; event.detail.state is an independent snapshot.\n// controller.state returns a copy. reset() restores the package defaults without an event.\n// destroy() removes listeners and stops CSS motion; repeated calls are safe.\n// Repeated mount on the same root returns its existing controller until destroyed.\n","selection-preview-mode-v11/example.js":"// Standalone demonstration: all selections remain local to this component.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// In your app, replace the call above with:\n// const controller = SLComponent.mount(root, { onChange(state) { /* Connect your setting here. */ } });\n// draft previews list or outline; applied holds the locally accepted mode. Apply copies draft to applied. Cancel and Escape restore draft from applied. selectionchange reports both fields; only applied should drive a committed application layout. No persistence is performed.\n// Initial state: { draft: \"list\", applied: \"list\" }\n// controller.setState(patch) validates and updates silently; pass { emit: true } to notify.\n// selectionchange bubbles across Shadow DOM; event.detail.state is an independent snapshot.\n// controller.state returns a copy. reset() restores the package defaults without an event.\n// destroy() removes listeners and stops CSS motion; repeated calls are safe.\n// Repeated mount on the same root returns its existing controller until destroyed.\n","selection-output-priority-v11/example.js":"// Standalone demonstration: all selections remain local to this component.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// In your app, replace the call above with:\n// const controller = SLComponent.mount(root, { onChange(state) { /* Connect your setting here. */ } });\n// primary is headphones or speakers; the other output is the fallback. Boolean fallback enables the second choice and the Swap priority action. This selects a preference only; it does not route audio or enumerate devices.\n// Initial state: { primary: \"headphones\", fallback: true }\n// controller.setState(patch) validates and updates silently; pass { emit: true } to notify.\n// selectionchange bubbles across Shadow DOM; event.detail.state is an independent snapshot.\n// controller.state returns a copy. reset() restores the package defaults without an event.\n// destroy() removes listeners and stops CSS motion; repeated calls are safe.\n// Repeated mount on the same root returns its existing controller until destroyed.\n","checkboxes-limited-v11/example.js":"// Local selection only. This example makes no requests or persistent changes.\nconst selection = SLComponent.mountPreview(document.querySelector('.sl-component'));\n// Application usage: SLComponent.mount(root, { values: [...], onChange(detail) { ... } }).\n// Keep the form root; do not nest it inside another form. Native radio names are isolated by this form.\n// detail contains action, kind and a copied values array. selectionchange carries the same fields.\n// setValues(values) validates and updates silently; state returns a fresh snapshot.\n// reset() restores HTML defaults silently; destroy() removes every listener and permits remounting.\n// At most three values are accepted. Unselected checkboxes are disabled at the limit; selected options always remain removable.\n// state.limit is 3.\n","checkboxes-budget-v11/example.js":"// Local selection only. This example makes no requests or persistent changes.\nconst selection = SLComponent.mountPreview(document.querySelector('.sl-component'));\n// Application usage: SLComponent.mount(root, { values: [...], onChange(detail) { ... } }).\n// Keep the form root; do not nest it inside another form. Native radio names are isolated by this form.\n// detail contains action, kind and a copied values array. selectionchange carries the same fields.\n// setValues(values) validates and updates silently; state returns a fresh snapshot.\n// reset() restores HTML defaults silently; destroy() removes every listener and permits remounting.\n// Costs come from data-cost. The fixed budget is eight points. An option is disabled when its cost exceeds the remaining budget; selected options remain removable. state includes budget and used.\n","checkboxes-permissions-v11/example.js":"// Local selection only. This example makes no requests or persistent changes.\nconst selection = SLComponent.mountPreview(document.querySelector('.sl-component'));\n// Application usage: SLComponent.mount(root, { values: [...], onChange(detail) { ... } }).\n// Keep the form root; do not nest it inside another form. Native radio names are isolated by this form.\n// detail contains action, kind and a copied values array. selectionchange carries the same fields.\n// setValues(values) validates and updates silently; state returns a fresh snapshot.\n// reset() restores HTML defaults silently; destroy() removes every listener and permits remounting.\n// Values use resource-view and resource-edit. User edits add view automatically. Clearing view also clears edit. setValues rejects edit without view.\n","checkboxes-group-quotas-v11/example.js":"// Local selection only. This example makes no requests or persistent changes.\nconst selection = SLComponent.mountPreview(document.querySelector('.sl-component'));\n// Application usage: SLComponent.mount(root, { values: [...], onChange(detail) { ... } }).\n// Keep the form root; do not nest it inside another form. Native radio names are isolated by this form.\n// detail contains action, kind and a copied values array. selectionchange carries the same fields.\n// setValues(values) validates and updates silently; state returns a fresh snapshot.\n// reset() restores HTML defaults silently; destroy() removes every listener and permits remounting.\n// Each data-group accepts up to two values. Filling one group never disables another. setValues rejects an overfilled group.\n","checkboxes-filtered-bulk-v11/example.js":"// Local selection only. This example makes no requests or persistent changes.\nconst selection = SLComponent.mountPreview(document.querySelector('.sl-component'));\n// Application usage: SLComponent.mount(root, { values: [...], onChange(detail) { ... } }).\n// Keep the form root; do not nest it inside another form. Native radio names are isolated by this form.\n// detail contains action, kind and a copied values array. selectionchange carries the same fields.\n// setValues(values) validates and updates silently; state returns a fresh snapshot.\n// reset() restores HTML defaults silently; destroy() removes every listener and permits remounting.\n// The tri-state master applies only to visible results. Filtering never changes selection. setFilter(text) updates the literal filter without a selection event. state includes query and visible values.\n// setFilter returns true while mounted, or false after destroy.\n","checkboxes-ranked-v11/example.js":"// Local selection only. This example makes no requests or persistent changes.\nconst selection = SLComponent.mountPreview(document.querySelector('.sl-component'));\n// Application usage: SLComponent.mount(root, { values: [...], onChange(detail) { ... } }).\n// Keep the form root; do not nest it inside another form. Native radio names are isolated by this form.\n// detail contains action, kind and a copied values array. selectionchange carries the same fields.\n// setValues(values) validates and updates silently; state returns a fresh snapshot.\n// reset() restores HTML defaults silently; destroy() removes every listener and permits remounting.\n// state.values preserves priority order. Checking appends a priority; unchecking removes its rank. moveEarlier(value) swaps it with the preceding item and emits a rank action. The first rank cannot move earlier.\n// state.limit is 3. moveEarlier returns true for a move; otherwise it returns false.\n","checkboxes-exclusive-none-v11/example.js":"// Local selection only. This example makes no requests or persistent changes.\nconst selection = SLComponent.mountPreview(document.querySelector('.sl-component'));\n// Application usage: SLComponent.mount(root, { values: [...], onChange(detail) { ... } }).\n// Keep the form root; do not nest it inside another form. Native radio names are isolated by this form.\n// detail contains action, kind and a copied values array. selectionchange carries the same fields.\n// setValues(values) validates and updates silently; state returns a fresh snapshot.\n// reset() restores HTML defaults silently; destroy() removes every listener and permits remounting.\n// The none value is exclusive. Choosing any channel clears none; choosing none clears all channels. Unchecking none leaves an empty, unspecified preference.\n","checkboxes-dependencies-v11/example.js":"// Local selection only. This example makes no requests or persistent changes.\nconst selection = SLComponent.mountPreview(document.querySelector('.sl-component'));\n// Application usage: SLComponent.mount(root, { values: [...], onChange(detail) { ... } }).\n// Keep the form root; do not nest it inside another form. Native radio names are isolated by this form.\n// detail contains action, kind and a copied values array. selectionchange carries the same fields.\n// setValues(values) validates and updates silently; state returns a fresh snapshot.\n// reset() restores HTML defaults silently; destroy() removes every listener and permits remounting.\n// Choosing comments or history includes sources. Clearing sources also clears both dependent details. Readme is independent. setValues rejects dependent details without sources.\n","checkboxes-optional-radio-v11/example.js":"// Local selection only. This example makes no requests or persistent changes.\nconst selection = SLComponent.mountPreview(document.querySelector('.sl-component'));\n// Application usage: SLComponent.mount(root, { values: [...], onChange(detail) { ... } }).\n// Keep the form root; do not nest it inside another form. Native radio names are isolated by this form.\n// detail contains action, kind and a copied values array. selectionchange carries the same fields.\n// setValues(values) validates and updates silently; state returns a fresh snapshot.\n// reset() restores HTML defaults silently; destroy() removes every listener and permits remounting.\n// Accepts zero or one delivery value. Arrow keys follow native radio behavior; Clear removes the active radio. state.credits is the local cost, not a purchase.\n","checkboxes-filter-polarity-v11/example.js":"// Local selection only. This example makes no requests or persistent changes.\nconst selection = SLComponent.mountPreview(document.querySelector('.sl-component'));\n// Application usage: SLComponent.mount(root, { values: [...], onChange(detail) { ... } }).\n// Keep the form root; do not nest it inside another form. Native radio names are isolated by this form.\n// detail contains action, kind and a copied values array. selectionchange carries the same fields.\n// setValues(values) validates and updates silently; state returns a fresh snapshot.\n// reset() restores HTML defaults silently; destroy() removes every listener and permits remounting.\n// Each of video, audio and text requires one value ending in :any, :include or :exclude. Clear returns all rules to any. Arrow keys operate within the focused native radio group.\n","sliders-loop-v11/example.js":"// Local selection demo. No network requests or audio/device operations occur.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n// Application integration: keep the returned controller in your own scope.\n// const controller = SLComponent.mount(root, { values: [24,48],\n//   onChange(state) { /* Apply the numeric state to your real application. */ }\n// });\n// values are [start, end] in seconds, rounded to 0.5 s, within 0–120 s and at least 1 s apart. state.duration and state.locked report the interval. Lock length keeps duration fixed during user edits; setValues replaces the interval explicitly.\n// controller.state returns a fresh snapshot. setValues(array) updates silently.\n// setDisabled(boolean) affects every control. reset() restores mount options.\n// destroy() removes listeners; calling mount again replaces an earlier mount.\n// User changes emit a bubbling, composed sliderchange event with state in detail.\n","sliders-crossfade-v11/example.js":"// Local selection demo. No network requests or audio/device operations occur.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n// Application integration: keep the returned controller in your own scope.\n// const controller = SLComponent.mount(root, { values: [50],\n//   onChange(state) { /* Apply the numeric state to your real application. */ }\n// });\n// values is [mixPercent], from 0 (source A) to 100 (source B). state.gains contains linear amplitude factors; state.equalPower selects sine/cosine or linear interpolation. Connect gains to your own audio graph.\n// controller.state returns a fresh snapshot. setValues(array) updates silently.\n// setDisabled(boolean) affects every control. reset() restores mount options.\n// destroy() removes listeners; calling mount again replaces an earlier mount.\n// User changes emit a bubbling, composed sliderchange event with state in detail.\n","sliders-gain-v11/example.js":"// Local selection demo. No network requests or audio/device operations occur.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n// Application integration: keep the returned controller in your own scope.\n// const controller = SLComponent.mount(root, { values: [-6],\n//   onChange(state) { /* Apply the numeric state to your real application. */ }\n// });\n// values is [gainDb], from −60 to +12 in 0.1 dB steps. state.amplitude is 10^(gainDb/20), or zero when state.muted is true. Mute preserves the chosen gain; no audio engine is included.\n// controller.state returns a fresh snapshot. setValues(array) updates silently.\n// setDisabled(boolean) affects every control. reset() restores mount options.\n// destroy() removes listeners; calling mount again replaces an earlier mount.\n// User changes emit a bubbling, composed sliderchange event with state in detail.\n","sliders-allocation-v11/example.js":"// Local selection demo. No network requests or audio/device operations occur.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n// Application integration: keep the returned controller in your own scope.\n// const controller = SLComponent.mount(root, { values: [40,35],\n//   onChange(state) { /* Apply the numeric state to your real application. */ }\n// });\n// values is [designHours, engineeringHours] in 1-hour steps. Each discipline and the remaining QA allocation keep at least 10 hours. state.hours contains all three allocations and state.total is always 100.\n// controller.state returns a fresh snapshot. setValues(array) updates silently.\n// setDisabled(boolean) affects every control. reset() restores mount options.\n// destroy() removes listeners; calling mount again replaces an earlier mount.\n// User changes emit a bubbling, composed sliderchange event with state in detail.\n","sliders-gradient-stops-v11/example.js":"// Local selection demo. No network requests or audio/device operations occur.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n// Application integration: keep the returned controller in your own scope.\n// const controller = SLComponent.mount(root, { values: [10,50,90],\n//   onChange(state) { /* Apply the numeric state to your real application. */ }\n// });\n// values contains the three ordered percentages. Stops retain a 5% gap. state.gradient is a CSS linear-gradient referencing --sl-stop-shade, --sl-stop-sage and --sl-stop-light; define the same variables wherever the gradient is used.\n// controller.state returns a fresh snapshot. setValues(array) updates silently.\n// setDisabled(boolean) affects every control. reset() restores mount options.\n// destroy() removes listeners; calling mount again replaces an earlier mount.\n// User changes emit a bubbling, composed sliderchange event with state in detail.\n","sliders-frequency-band-v11/example.js":"// Local selection demo. No network requests or audio/device operations occur.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n// Application integration: keep the returned controller in your own scope.\n// const controller = SLComponent.mount(root, { values: [40,80],\n//   onChange(state) { /* Apply the numeric state to your real application. */ }\n// });\n// values contains logarithmic positions 0–120, in steps of 1. Frequency in Hz is 20 × 1000^(position/120); state.frequenciesHz exposes unrounded frequencies and state.octaves the interval. Native accessible value text always announces Hz. Cutoffs retain one logarithmic step of separation.\n// controller.state returns a fresh snapshot. setValues(array) updates silently.\n// setDisabled(boolean) affects every control. reset() restores mount options.\n// destroy() removes listeners; calling mount again replaces an earlier mount.\n// User changes emit a bubbling, composed sliderchange event with state in detail.\n","sliders-envelope-v11/example.js":"// Local selection demo. No network requests or audio/device operations occur.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n// Application integration: keep the returned controller in your own scope.\n// const controller = SLComponent.mount(root, { values: [100,250,60,500],\n//   onChange(state) { /* Apply the numeric state to your real application. */ }\n// });\n// values is [attackMs, decayMs, sustainPercent, releaseMs]. Time stages step by 10 ms. state.sustain is the 0–1 amplitude. The graph includes a fixed, explicitly labelled 500 ms hold; no audio or playback timer is included.\n// controller.state returns a fresh snapshot. setValues(array) updates silently.\n// setDisabled(boolean) affects every control. reset() restores mount options.\n// destroy() removes listeners; calling mount again replaces an earlier mount.\n// User changes emit a bubbling, composed sliderchange event with state in detail.\n","sliders-dead-zone-v11/example.js":"// Local selection demo. No network requests or audio/device operations occur.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n// Application integration: keep the returned controller in your own scope.\n// const controller = SLComponent.mount(root, { values: [12,90,35],\n//   onChange(state) { /* Apply the numeric state to your real application. */ }\n// });\n// values is [innerPercent, outerPercent, signedTestPercent]. Inner is 0–40%, outer 60–100%, test −100–100%. state.response is signed output from −1 to 1 after dead-zone removal and rescaling; integration with a device is application-owned.\n// controller.state returns a fresh snapshot. setValues(array) updates silently.\n// setDisabled(boolean) affects every control. reset() restores mount options.\n// destroy() removes listeners; calling mount again replaces an earlier mount.\n// User changes emit a bubbling, composed sliderchange event with state in detail.\n","sliders-tolerance-v11/example.js":"// Local selection demo. No network requests or audio/device operations occur.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n// Application integration: keep the returned controller in your own scope.\n// const controller = SLComponent.mount(root, { values: [50,2,51.4],\n//   onChange(state) { /* Apply the numeric state to your real application. */ }\n// });\n// values is [nominalMm, toleranceMm, sampleMm] in 0.1 mm steps. User edits preserve the tolerance while clamping the nominal; setValues clamps the requested tolerance to the new nominal. state.lower, upper, deviation and accepted describe the inclusive test.\n// controller.state returns a fresh snapshot. setValues(array) updates silently.\n// setDisabled(boolean) affects every control. reset() restores mount options.\n// destroy() removes listeners; calling mount again replaces an earlier mount.\n// User changes emit a bubbling, composed sliderchange event with state in detail.\n","sliders-quantity-v11/example.js":"// Local preview only. No purchases, network requests or saved settings.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n// values is [quantity], an integer from 1 to 20. state.quantity mirrors it; state.unitPriceCents is the fixed demonstration price 1200 and subtotalCents is quantity × 1200, currency USD. Use your own prices and real checkout separately. No order is submitted.\n// const controller = SLComponent.mount(root, { values: [2], onChange(state) {\n//   // Apply the value to your own app after validating application constraints.\n// } });\n// controller.state is a fresh snapshot; setValues(array) is silent.\n// setDisabled(boolean), reset() and destroy() follow the library lifecycle.\n// User edits emit a bubbling, composed sliderchange event.\n","sliders-line-height-v11/example.js":"// Local preview only. No purchases, network requests or saved settings.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n// values is [lineHeight], a unitless CSS line-height from 1.2 to 2 in 0.1 steps. state.lineHeight is directly usable on the target paragraph; typography outside this component is not changed. The paragraph is ordinary text, not an image.\n// const controller = SLComponent.mount(root, { values: [1.5], onChange(state) {\n//   // Apply the value to your own app after validating application constraints.\n// } });\n// controller.state is a fresh snapshot; setValues(array) is silent.\n// setDisabled(boolean), reset() and destroy() follow the library lifecycle.\n// User edits emit a bubbling, composed sliderchange event.\n","sliders-content-width-v11/example.js":"// Local preview only. No purchases, network requests or saved settings.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n// values is [widthPercent], 50–100 in steps of 5. state.widthCss is a percentage width for a centered column in its own parent. The demo changes an actual text column without changing font size. Apply margin-inline:auto to your target column.\n// const controller = SLComponent.mount(root, { values: [80], onChange(state) {\n//   // Apply the value to your own app after validating application constraints.\n// } });\n// controller.state is a fresh snapshot; setValues(array) is silent.\n// setDisabled(boolean), reset() and destroy() follow the library lifecycle.\n// User edits emit a bubbling, composed sliderchange event.\n","sliders-card-gap-v11/example.js":"// Local preview only. No purchases, network requests or saved settings.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n// values is [gapPx], from 4 to 24 in steps of 2. state.gapCss is a CSS gap value for a grid or flex container. Each preview card keeps its own padding and font size; only space between cards changes.\n// const controller = SLComponent.mount(root, { values: [12], onChange(state) {\n//   // Apply the value to your own app after validating application constraints.\n// } });\n// controller.state is a fresh snapshot; setValues(array) is silent.\n// setDisabled(boolean), reset() and destroy() follow the library lifecycle.\n// User edits emit a bubbling, composed sliderchange event.\n","sliders-thumbnails-v11/example.js":"// Local preview only. No purchases, network requests or saved settings.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n// values is [thumbnailPx], from 48 to 88 in steps of 4. state.thumbnailCss is a CSS width. The local gallery keeps a 4:3 aspect ratio and wraps using flexbox; the sample illustrations are inline SVG. Replace them with your own images and meaningful alt text.\n// const controller = SLComponent.mount(root, { values: [64], onChange(state) {\n//   // Apply the value to your own app after validating application constraints.\n// } });\n// controller.state is a fresh snapshot; setValues(array) is silent.\n// setDisabled(boolean), reset() and destroy() follow the library lifecycle.\n// User edits emit a bubbling, composed sliderchange event.\n","menus-resource-picker-v11/example.js":"// This demonstration changes local state only and makes no requests.\nconst root = document.querySelector('.sl-component');\nconst controller = SLComponent.mountPreview(root);\n// Application usage: SLComponent.mount(root, { onChange(state) { /* Read local state. */ } });\n// Call controller.destroy() before remounting with different options.\n// controller.open(), controller.close(), controller.reset(), controller.state and controller.destroy() are available.\n// Listen for the bubbling, composed sl-menu-change event to read event.detail.\n// Read value (atlas, field, brief or guide) and query from onChange. Selection changes only the local resource summary.\n","menus-nested-topic-v11/example.js":"// This demonstration changes local state only and makes no requests.\nconst root = document.querySelector('.sl-component');\nconst controller = SLComponent.mountPreview(root);\n// Application usage: SLComponent.mount(root, { onChange(state) { /* Read local state. */ } });\n// Call controller.destroy() before remounting with different options.\n// controller.open(), controller.close(), controller.reset(), controller.state and controller.destroy() are available.\n// Listen for the bubbling, composed sl-menu-change event to read event.detail.\n// value is the complete leaf path; path is the current browsing path. ArrowRight enters a branch; ArrowLeft and Back move to its parent.\n","menus-recent-actions-v11/example.js":"// This demonstration changes local state only and makes no requests.\nconst root = document.querySelector('.sl-component');\nconst controller = SLComponent.mountPreview(root);\n// Application usage: SLComponent.mount(root, { onChange(state) { /* Read local state. */ } });\n// Call controller.destroy() before remounting with different options.\n// controller.open(), controller.close(), controller.reset(), controller.state and controller.destroy() are available.\n// Listen for the bubbling, composed sl-menu-change event to read event.detail.\n// zoom is clamped to 50–150; pinned is a boolean; recent contains up to three unique action IDs. Clear recents preserves the canvas state.\n","menus-tag-workbench-v11/example.js":"// This demonstration changes local state only and makes no requests.\nconst root = document.querySelector('.sl-component');\nconst controller = SLComponent.mountPreview(root);\n// Application usage: SLComponent.mount(root, { onChange(state) { /* Read local state. */ } });\n// Call controller.destroy() before remounting with different options.\n// controller.open(), controller.close(), controller.reset(), controller.state and controller.destroy() are available.\n// Listen for the bubbling, composed sl-menu-change event to read event.detail.\n// tags and selected are committed strings; draftTags and draft are temporary. Cancel, Escape, blur and outside click discard the draft. Creation permits eight tags, 24 Unicode characters each, and ignores case-insensitive duplicates.\n","menus-branch-picker-v11/example.js":"// This demonstration changes local state only and makes no requests.\nconst root = document.querySelector('.sl-component');\nconst controller = SLComponent.mountPreview(root);\n// Application usage: SLComponent.mount(root, { onChange(state) { /* Read local state. */ } });\n// Call controller.destroy() before remounting with different options.\n// controller.open(), controller.close(), controller.reset(), controller.state and controller.destroy() are available.\n// Listen for the bubbling, composed sl-menu-change event to read event.detail.\n// value is a branch or tag ID. tab and query control browsing, while compare displays fixed sample ahead/behind counts. No checkout or repository operation is performed.\n","menus-weekly-window-v11/example.js":"// This demonstration changes local state only and makes no requests.\nconst root = document.querySelector('.sl-component');\nconst controller = SLComponent.mountPreview(root);\n// Application usage: SLComponent.mount(root, { onChange(state) { /* Read local state. */ } });\n// Call controller.destroy() before remounting with different options.\n// controller.open(), controller.close(), controller.reset(), controller.state and controller.destroy() are available.\n// Listen for the bubbling, composed sl-menu-change event to read event.detail.\n// days contains Monday-based indices 0–6 and time is HH:MM in local 24-hour time. Apply requires at least one day and a valid time. Dismissal discards draftDays and draftTime; scheduling remains the host application’s responsibility.\n","menus-sort-recipe-v11/example.js":"// This demonstration changes local state only and makes no requests.\nconst root = document.querySelector('.sl-component');\nconst controller = SLComponent.mountPreview(root);\n// Application usage: SLComponent.mount(root, { onChange(state) { /* Read local state. */ } });\n// Call controller.destroy() before remounting with different options.\n// controller.open(), controller.close(), controller.reset(), controller.state and controller.destroy() are available.\n// Listen for the bubbling, composed sl-menu-change event to read event.detail.\n// field is due or name, direction is 1 or -1, and priority groups High, Medium and Low first; field and direction sort within equal-priority groups. Sample task ordering is computed from numeric due dates and priority values.\n","menus-column-visibility-v11/example.js":"// This demonstration changes local state only and makes no requests.\nconst root = document.querySelector('.sl-component');\nconst controller = SLComponent.mountPreview(root);\n// Application usage: SLComponent.mount(root, { onChange(state) { /* Read local state. */ } });\n// Call controller.destroy() before remounting with different options.\n// controller.open(), controller.close(), controller.reset(), controller.state and controller.destroy() are available.\n// Listen for the bubbling, composed sl-menu-change event to read event.detail.\n// columns contains ordered IDs name, status, owner and due. name is required and cannot be removed. Compact restores name and status; Show all reveals all four columns.\n","menus-checkpoint-menu-v11/example.js":"// This demonstration changes local state only and makes no requests.\nconst root = document.querySelector('.sl-component');\nconst controller = SLComponent.mountPreview(root);\n// Application usage: SLComponent.mount(root, { onChange(state) { /* Read local state. */ } });\n// Call controller.destroy() before remounting with different options.\n// controller.open(), controller.close(), controller.reset(), controller.state and controller.destroy() are available.\n// Listen for the bubbling, composed sl-menu-change event to read event.detail.\n// current and draft identify v1, v2 or v3; previous supports one undo. Choosing a checkpoint only previews it. Use version changes the sample text and Undo restore reverses that change.\n","menus-variable-insert-v11/example.js":"// This demonstration changes local state only and makes no requests.\nconst root = document.querySelector('.sl-component');\nconst controller = SLComponent.mountPreview(root);\n// Application usage: SLComponent.mount(root, { onChange(state) { /* Read local state. */ } });\n// Call controller.destroy() before remounting with different options.\n// controller.open(), controller.close(), controller.reset(), controller.state and controller.destroy() are available.\n// Listen for the bubbling, composed sl-menu-change event to read event.detail.\n// text holds literal editor content; start and end are UTF-16 selection offsets used by textarea. A token replaces that selection. previous supports one undo and manual edits clear undo. Account ID is intentionally unavailable.\n","navigation-overflow-tabs-v11/example.js":"// All navigation in this example changes local content only.\n// For an application, call SLComponent.mount(root, { onNavigate(detail) { ... } }).\n// Use the event detail to connect your router explicitly; no URL or server is changed here.\n// Listen for \"sl-navigate\" on root instead of onNavigate if event delegation is preferred.\n// All six sections stay visible in one wrapping tab group, using navigate(index).\n(() => {\n  const root = document.querySelector('.sl-component');\n  const controller = SLComponent.mountPreview(root);\n  window.addEventListener('pagehide', event => {\n    if (!event.persisted) controller.destroy();\n  }, { once: true });\n})();\n","navigation-drill-down-v11/example.js":"// All navigation in this example changes local content only.\n// For an application, call SLComponent.mount(root, { onNavigate(detail) { ... } }).\n// Use the event detail to connect your router explicitly; no URL or server is changed here.\n// Listen for \"sl-navigate\" on root instead of onNavigate if event delegation is preferred.\n(() => {\n  const root = document.querySelector('.sl-component');\n  const controller = SLComponent.mountPreview(root);\n  window.addEventListener('pagehide', event => {\n    if (!event.persisted) controller.destroy();\n  }, { once: true });\n})();\n","navigation-history-v11/example.js":"// All navigation in this example changes local content only.\n// For an application, call SLComponent.mount(root, { onNavigate(detail) { ... } }).\n// Use the event detail to connect your router explicitly; no URL or server is changed here.\n// Listen for \"sl-navigate\" on root instead of onNavigate if event delegation is preferred.\n(() => {\n  const root = document.querySelector('.sl-component');\n  const controller = SLComponent.mountPreview(root);\n  window.addEventListener('pagehide', event => {\n    if (!event.persisted) controller.destroy();\n  }, { once: true });\n})();\n","navigation-context-rail-v11/example.js":"// All navigation in this example changes local content only.\n// For an application, call SLComponent.mount(root, { onNavigate(detail) { ... } }).\n// Use the event detail to connect your router explicitly; no URL or server is changed here.\n// Listen for \"sl-navigate\" on root instead of onNavigate if event delegation is preferred.\n(() => {\n  const root = document.querySelector('.sl-component');\n  const controller = SLComponent.mountPreview(root);\n  window.addEventListener('pagehide', event => {\n    if (!event.persisted) controller.destroy();\n  }, { once: true });\n})();\n","navigation-landmark-jump-v11/example.js":"// All navigation in this example changes local content only.\n// For an application, call SLComponent.mount(root, { onNavigate(detail) { ... } }).\n// Use the event detail to connect your router explicitly; no URL or server is changed here.\n// Listen for \"sl-navigate\" on root instead of onNavigate if event delegation is preferred.\n(() => {\n  const root = document.querySelector('.sl-component');\n  const controller = SLComponent.mountPreview(root);\n  window.addEventListener('pagehide', event => {\n    if (!event.persisted) controller.destroy();\n  }, { once: true });\n})();\n","navigation-route-finder-v11/example.js":"// All navigation in this example changes local content only.\n// For an application, call SLComponent.mount(root, { onNavigate(detail) { ... } }).\n// Use the event detail to connect your router explicitly; no URL or server is changed here.\n// Listen for \"sl-navigate\" on root instead of onNavigate if event delegation is preferred.\n(() => {\n  const root = document.querySelector('.sl-component');\n  const controller = SLComponent.mountPreview(root);\n  window.addEventListener('pagehide', event => {\n    if (!event.persisted) controller.destroy();\n  }, { once: true });\n})();\n","navigation-alphabet-jump-v11/example.js":"// All navigation in this example changes local content only.\n// For an application, call SLComponent.mount(root, { onNavigate(detail) { ... } }).\n// Use the event detail to connect your router explicitly; no URL or server is changed here.\n// Listen for \"sl-navigate\" on root instead of onNavigate if event delegation is preferred.\n(() => {\n  const root = document.querySelector('.sl-component');\n  const controller = SLComponent.mountPreview(root);\n  window.addEventListener('pagehide', event => {\n    if (!event.persisted) controller.destroy();\n  }, { once: true });\n})();\n","navigation-reference-return-v11/example.js":"// All navigation in this example changes local content only.\n// For an application, call SLComponent.mount(root, { onNavigate(detail) { ... } }).\n// Use the event detail to connect your router explicitly; no URL or server is changed here.\n// Listen for \"sl-navigate\" on root instead of onNavigate if event delegation is preferred.\n(() => {\n  const root = document.querySelector('.sl-component');\n  const controller = SLComponent.mountPreview(root);\n  window.addEventListener('pagehide', event => {\n    if (!event.persisted) controller.destroy();\n  }, { once: true });\n})();\n","navigation-reading-queue-v11/example.js":"// All navigation in this example changes local content only.\n// For an application, call SLComponent.mount(root, { onNavigate(detail) { ... } }).\n// Use the event detail to connect your router explicitly; no URL or server is changed here.\n// Listen for \"sl-navigate\" on root instead of onNavigate if event delegation is preferred.\n// complete() marks the current unread document read and advances. markUnread() marks the selected read document unread.\n// undo() restores up to twenty reading changes in reverse order; ordinary navigation keeps that history.\n(() => {\n  const root = document.querySelector('.sl-component');\n  const controller = SLComponent.mountPreview(root);\n  window.addEventListener('pagehide', event => {\n    if (!event.persisted) controller.destroy();\n  }, { once: true });\n})();\n","navigation-spatial-map-v11/example.js":"// All navigation in this example changes local content only.\n// For an application, call SLComponent.mount(root, { onNavigate(detail) { ... } }).\n// Use the event detail to connect your router explicitly; no URL or server is changed here.\n// Listen for \"sl-navigate\" on root instead of onNavigate if event delegation is preferred.\n(() => {\n  const root = document.querySelector('.sl-component');\n  const controller = SLComponent.mountPreview(root);\n  window.addEventListener('pagehide', event => {\n    if (!event.persisted) controller.destroy();\n  }, { once: true });\n})();\n","overlays-workspace-settings-v11/example.js":"// This example changes local state only and makes no remote requests.\n// Application use: SLComponent.mount(root, { onChange(detail) { /* consume detail.value */ } }).\n// onChange is a local notification, never a server success callback.\n// Keep the controller in application scope; call destroy() before removing the root.\n(() => {\n  const root = document.querySelector('.sl-component');\n  const controller = SLComponent.mountPreview(root);\n  window.addEventListener('pagehide', event => { if (!event.persisted) controller.destroy(); });\n})();\n","overlays-date-picker-v11/example.js":"// This example changes local state only and makes no remote requests.\n// Application use: SLComponent.mount(root, { onChange(detail) { /* consume detail.value */ } }).\n// onChange is a local notification, never a server success callback.\n// Keep the controller in application scope; call destroy() before removing the root.\n(() => {\n  const root = document.querySelector('.sl-component');\n  const controller = SLComponent.mountPreview(root);\n  window.addEventListener('pagehide', event => { if (!event.persisted) controller.destroy(); });\n})();\n","overlays-notification-inbox-v11/example.js":"// This example changes local state only and makes no remote requests.\n// Application use: SLComponent.mount(root, { onChange(detail) { /* consume detail.value */ } }).\n// onChange is a local notification, never a server success callback.\n// Keep the controller in application scope; call destroy() before removing the root.\n(() => {\n  const root = document.querySelector('.sl-component');\n  const controller = SLComponent.mountPreview(root);\n  window.addEventListener('pagehide', event => { if (!event.persisted) controller.destroy(); });\n})();\n","overlays-plan-compare-v11/example.js":"// This example changes local state only and makes no remote requests.\n// Application use: SLComponent.mount(root, { onChange(detail) { /* consume detail.value */ } }).\n// onChange is a local notification, never a server success callback.\n// Keep the controller in application scope; call destroy() before removing the root.\n(() => {\n  const root = document.querySelector('.sl-component');\n  const controller = SLComponent.mountPreview(root);\n  window.addEventListener('pagehide', event => { if (!event.persisted) controller.destroy(); });\n})();\n","overlays-style-inspector-v11/example.js":"// This example changes local state only and makes no remote requests.\n// Application use: SLComponent.mount(root, { onChange(detail) { /* consume detail.value */ } }).\n// onChange is a local notification, never a server success callback.\n// Keep the controller in application scope; call destroy() before removing the root.\n(() => {\n  const root = document.querySelector('.sl-component');\n  const controller = SLComponent.mountPreview(root);\n  window.addEventListener('pagehide', event => { if (!event.persisted) controller.destroy(); });\n})();\n","overlays-shortcut-guide-v11/example.js":"// This example changes local state only and makes no remote requests.\n// Application use: SLComponent.mount(root, { onChange(detail) { /* consume detail.value */ } }).\n// onChange is a local notification, never a server success callback.\n// Keep the controller in application scope; call destroy() before removing the root.\n(() => {\n  const root = document.querySelector('.sl-component');\n  const controller = SLComponent.mountPreview(root);\n  window.addEventListener('pagehide', event => { if (!event.persisted) controller.destroy(); });\n})();\n","overlays-filter-builder-v11/example.js":"// This example changes local state only and makes no remote requests.\n// Application use: SLComponent.mount(root, { onChange(detail) { /* consume detail.value */ } }).\n// onChange is a local notification, never a server success callback.\n// Keep the controller in application scope; call destroy() before removing the root.\n(() => {\n  const root = document.querySelector('.sl-component');\n  const controller = SLComponent.mountPreview(root);\n  window.addEventListener('pagehide', event => { if (!event.persisted) controller.destroy(); });\n})();\n","overlays-color-mixer-v11/example.js":"// This example changes local state only and makes no remote requests.\n// Application use: SLComponent.mount(root, { onChange(detail) { /* consume detail.value */ } }).\n// onChange is a local notification, never a server success callback.\n// Keep the controller in application scope; call destroy() before removing the root.\n(() => {\n  const root = document.querySelector('.sl-component');\n  const controller = SLComponent.mountPreview(root);\n  window.addEventListener('pagehide', event => { if (!event.persisted) controller.destroy(); });\n})();\n","overlays-focus-timer-v11/example.js":"// This example changes local state only and makes no remote requests.\n// Application use: SLComponent.mount(root, { onChange(detail) { /* consume detail.value */ } }).\n// onChange is a local notification, never a server success callback.\n// Keep the controller in application scope; call destroy() before removing the root.\n(() => {\n  const root = document.querySelector('.sl-component');\n  const controller = SLComponent.mountPreview(root);\n  window.addEventListener('pagehide', event => { if (!event.persisted) controller.destroy(); });\n})();\n","overlays-version-history-v11/example.js":"// This example changes local state only and makes no remote requests.\n// Application use: SLComponent.mount(root, { onChange(detail) { /* consume detail.value */ } }).\n// onChange is a local notification, never a server success callback.\n// Keep the controller in application scope; call destroy() before removing the root.\n(() => {\n  const root = document.querySelector('.sl-component');\n  const controller = SLComponent.mountPreview(root);\n  window.addEventListener('pagehide', event => { if (!event.persisted) controller.destroy(); });\n})();\n","feedback-errors-v11/example.js":"// Explicit local demonstration. Replace this call for application integration.\nconst controller = SLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application API: validate() checks a nonblank name and an email with a dotted domain.\n// It returns true on format success, otherwise focuses the first error link.\n// state is { status, errors }; editing clears ready until the next explicit check.\n// reset() empties both fields. This checks format, not mailbox existence; nothing is sent.\n// Use SLComponent.mount(root, options). mount() never simulates remote success.\n// controller.reset() restores the initial view; controller.destroy() releases its listeners and work.\n","feedback-draft-v11/example.js":"// Explicit local demonstration. Replace this call for application integration.\nconst controller = SLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application API: Options: initialText and onAction(\"save\", { text, signal }). Resolve only after persistence succeeds; reject on failure. save() returns Promise<boolean>; discard() restores the last confirmed checkpoint, cancel() aborts the callback, and state exposes { status, dirty, text, saved }. reset() returns to initialText; destroy() aborts and detaches.\n// Use SLComponent.mount(root, options). mount() never simulates remote success.\n// controller.reset() restores the initial view; controller.destroy() releases its listeners and work.\n","feedback-session-v11/example.js":"// Explicit local demonstration. Replace this call for application integration.\nconst controller = SLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application API: Options: durationSeconds (1–86400; default 90) and onAction(\"renew\", { signal }). Renewal must return { expiresAt } with a future Unix timestamp in milliseconds. renew() returns Promise<boolean>. state exposes { status, expiresAt, expired }. The clock continues during pending renewal and uses wall time after a suspended tab. reset() creates a fresh local duration; destroy() clears the interval.\n// Use SLComponent.mount(root, options). mount() never simulates remote success.\n// controller.reset() restores the initial view; controller.destroy() releases its listeners and work.\n","feedback-capacity-v11/example.js":"// Explicit local demonstration. Replace this call for application integration.\nconst controller = SLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application API: Pure local capacity model: stage() adds the selected 4, 8 or 18 MB file only when it fits within 100 MB; release() frees the fixed 20 MB cache once; remove() clears all staged files. Each returns boolean. state is { status, used, staged, available }. No storage API or upload is implied. reset() restores the 86 MB baseline.\n// Use SLComponent.mount(root, options). mount() never simulates remote success.\n// controller.reset() restores the initial view; controller.destroy() releases its listeners and work.\n","feedback-conflict-v11/example.js":"// Explicit local demonstration. Replace this call for application integration.\nconst controller = SLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application API: Options: localText, remoteText and onAction(\"resolve\", { choice, text, signal }). User text is rendered literally. choose(\"local\"|\"remote\") selects a version; resolve() returns Promise<boolean> and freezes the choice only after callback fulfillment. cancel() leaves both versions intact. state exposes { status, choice, resolved, localText, remoteText }; reset() reopens the original conflict.\n// Use SLComponent.mount(root, options). mount() never simulates remote success.\n// controller.reset() restores the initial view; controller.destroy() releases its listeners and work.\n","feedback-batch-v11/example.js":"// Explicit local demonstration. Replace this call for application integration.\nconst controller = SLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application API: Options: results mapping notes, map and interview to \"done\", \"failed\" or \"skipped\"; onAction(\"retry\", { id, signal }) performs one unfinished item. retry() processes those items sequentially and stops at the first error or cancellation, retaining confirmed successes. dismiss() marks the receipt reviewed locally. state is { status, results, reviewed }. reset() restores supplied results.\n// Use SLComponent.mount(root, options). mount() never simulates remote success.\n// controller.reset() restores the initial view; controller.destroy() releases its listeners and work.\n","feedback-access-v11/example.js":"// Explicit local demonstration. Replace this call for application integration.\nconst controller = SLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application API: Provide onAction(\"request\"|\"check\", { reason, signal }) returning { status: \"pending\"|\"denied\"|\"granted\" }. request() validates a nonempty reason and switches to checking once pending. Rejection preserves access state; cancel() aborts work. state is { status, access, reason }. reset() returns to access required. No browser or operating-system permission is requested automatically.\n// Use SLComponent.mount(root, options). mount() never simulates remote success.\n// controller.reset() restores the initial view; controller.destroy() releases its listeners and work.\n","feedback-empty-v11/example.js":"// Explicit local demonstration. Replace this call for application integration.\nconst controller = SLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application API: This component searches three bundled sample files entirely locally. clear() removes the text constraint and widen() includes active files, with focus returned to the search field. Native search and checkbox controls support keyboard input. state exposes { status, query, archivedOnly, matches }. Adapt the local files array to your data or wire real search explicitly. reset() restores the zero-results example.\n// Use SLComponent.mount(root, options). mount() never simulates remote success.\n// controller.reset() restores the initial view; controller.destroy() releases its listeners and work.\n","feedback-incident-v11/example.js":"// Explicit local demonstration. Replace this call for application integration.\nconst controller = SLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application API: update({ phase, title, detail, label, time }) accepts investigating, identified or resolved and keeps the three latest history entries. Supply actual incident data from your application; this view never polls. acknowledge() toggles a local acknowledgement, disclosure(boolean) controls affected-service details, and Escape closes them. state exposes { status, phase, acknowledged, expanded, history }. reset() restores the sample incident.\n// Use SLComponent.mount(root, options). mount() never simulates remote success.\n// controller.reset() restores the initial view; controller.destroy() releases its listeners and work.\n","feedback-readiness-v11/example.js":"// Explicit local demonstration. Replace this call for application integration.\nconst controller = SLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application API: Provide onAction(\"check\", { id, signal }) for details, access and storage; return { ok: boolean }. check() runs checks sequentially and returns Promise<boolean> for all passed. A blocked check remains visible while the remaining checks run; rejection or cancellation stops the sequence. state is { status, checks }. cancel() keeps finished results; reset() clears all checks. No sharing operation occurs.\n// Use SLComponent.mount(root, options). mount() never simulates remote success.\n// controller.reset() restores the initial view; controller.destroy() releases its listeners and work.\n","data-display-funnel-v11/example.js":"// Honest local demonstration: selections do not save, fetch or submit data.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application usage: mount a separate root with SLComponent.mount(root, { onChange(detail) {} }).\n// detail = { kind, action, state }; also emitted as bubbling, composed \"displaychange\".\n// select(index) accepts funnel stages 0–3. setMode(\"entry\" | \"previous\") changes percentage denominators. First-stage conversion remains 100%; bar widths and labels use the selected denominator. state contains selected and mode.\n// reset() restores default visuals and ARIA. destroy() removes listeners; repeated calls are safe.\n// Call destroy() before removing the root. A later mount starts with defaults.\n// Each controller owns its state. CSS-only motion responds to live reduced-motion changes.\n// Replace sample text with textContent and keep numeric labels/scales synchronized.\n// Bind --dd-surface, --dd-text, --dd-muted, --dd-accent, --dd-selected, --dd-edge and --dd-focus to your palette.\n","data-display-score-matrix-v11/example.js":"// Honest local demonstration: selections do not save, fetch or submit data.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application usage: mount a separate root with SLComponent.mount(root, { onChange(detail) {} }).\n// detail = { kind, action, state }; also emitted as bubbling, composed \"displaychange\".\n// select(index) accepts cells 0–8 in row-major order (Speed, Usability, Coverage × Web, iOS, API). setMode(\"score\" | \"gap\") shows score or 5 minus score. state contains selected and mode.\n// reset() restores default visuals and ARIA. destroy() removes listeners; repeated calls are safe.\n// Call destroy() before removing the root. A later mount starts with defaults.\n// Each controller owns its state. CSS-only motion responds to live reduced-motion changes.\n// Replace sample text with textContent and keep numeric labels/scales synchronized.\n// Bind --dd-surface, --dd-text, --dd-muted, --dd-accent, --dd-selected, --dd-edge and --dd-focus to your palette.\n","data-display-treemap-v11/example.js":"// Honest local demonstration: selections do not save, fetch or submit data.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application usage: mount a separate root with SLComponent.mount(root, { onChange(detail) {} }).\n// detail = { kind, action, state }; also emitted as bubbling, composed \"displaychange\".\n// select(index) accepts 0 Assets, 1 Media, 2 Docs, 3 Logs. setMode(\"storage\" | \"files\") recomputes proportional areas against 100 GB or 800 files. state contains selected and mode.\n// reset() restores default visuals and ARIA. destroy() removes listeners; repeated calls are safe.\n// Call destroy() before removing the root. A later mount starts with defaults.\n// Each controller owns its state. CSS-only motion responds to live reduced-motion changes.\n// Replace sample text with textContent and keep numeric labels/scales synchronized.\n// Bind --dd-surface, --dd-text, --dd-muted, --dd-accent, --dd-selected, --dd-edge and --dd-focus to your palette.\n","data-display-comparison-v11/example.js":"// Honest local demonstration: selections do not save, fetch or submit data.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application usage: mount a separate root with SLComponent.mount(root, { onChange(detail) {} }).\n// detail = { kind, action, state }; also emitted as bubbling, composed \"displaychange\".\n// select(index) accepts teams 0–2. setMode(\"count\" | \"percent\") changes the delta labels; geometry always uses a fixed 0–160 issue scale. state contains selected and mode.\n// reset() restores default visuals and ARIA. destroy() removes listeners; repeated calls are safe.\n// Call destroy() before removing the root. A later mount starts with defaults.\n// Each controller owns its state. CSS-only motion responds to live reduced-motion changes.\n// Replace sample text with textContent and keep numeric labels/scales synchronized.\n// Bind --dd-surface, --dd-text, --dd-muted, --dd-accent, --dd-selected, --dd-edge and --dd-focus to your palette.\n","data-display-grouped-rows-v11/example.js":"// Honest local demonstration: selections do not save, fetch or submit data.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application usage: mount a separate root with SLComponent.mount(root, { onChange(detail) {} }).\n// detail = { kind, action, state }; also emitted as bubbling, composed \"displaychange\".\n// select(index) accepts entries 0–5 and opens their group. toggle(groupIndex, expanded?) accepts groups 0–2. Escape inside a group closes it and returns focus to its header. state includes selected and a frozen expanded array.\n// reset() restores default visuals and ARIA. destroy() removes listeners; repeated calls are safe.\n// Call destroy() before removing the root. A later mount starts with defaults.\n// Each controller owns its state. CSS-only motion responds to live reduced-motion changes.\n// Replace sample text with textContent and keep numeric labels/scales synchronized.\n// Bind --dd-surface, --dd-text, --dd-muted, --dd-accent, --dd-selected, --dd-edge and --dd-focus to your palette.\n","data-display-distribution-v11/example.js":"// Honest local demonstration: selections do not save, fetch or submit data.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application usage: mount a separate root with SLComponent.mount(root, { onChange(detail) {} }).\n// detail = { kind, action, state }; also emitted as bubbling, composed \"displaychange\".\n// select(index) chooses the five summary rows, 0–4. setMode(\"east\" | \"west\") updates labels and animated bars on a shared 0–100 ms scale. state contains selected and mode.\n// reset() restores default visuals and ARIA. destroy() removes listeners; repeated calls are safe.\n// Call destroy() before removing the root. A later mount starts with defaults.\n// Each controller owns its state. CSS-only motion responds to live reduced-motion changes.\n// Replace sample text with textContent and keep numeric labels/scales synchronized.\n// Bind --dd-surface, --dd-text, --dd-muted, --dd-accent, --dd-selected, --dd-edge and --dd-focus to your palette.\n","data-display-series-legend-v11/example.js":"// Honest local demonstration: selections do not save, fetch or submit data.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application usage: mount a separate root with SLComponent.mount(root, { onChange(detail) {} }).\n// detail = { kind, action, state }; also emitted as bubbling, composed \"displaychange\".\n// toggle(index, visible?) accepts 0 Direct, 1 Search, 2 Referral. It returns false if hiding the last visible series. state.visible is a frozen boolean array. Totals are visits summed across W1–W4.\n// reset() restores default visuals and ARIA. destroy() removes listeners; repeated calls are safe.\n// Call destroy() before removing the root. A later mount starts with defaults.\n// Each controller owns its state. CSS-only motion responds to live reduced-motion changes.\n// Replace sample text with textContent and keep numeric labels/scales synchronized.\n// Bind --dd-surface, --dd-text, --dd-muted, --dd-accent, --dd-selected, --dd-edge and --dd-focus to your palette.\n","data-display-heatmap-v11/example.js":"// Honest local demonstration: selections do not save, fetch or submit data.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application usage: mount a separate root with SLComponent.mount(root, { onChange(detail) {} }).\n// detail = { kind, action, state }; also emitted as bubbling, composed \"displaychange\".\n// select(index) accepts 0–14 in row-major order (09:00, 13:00, 17:00; Monday–Friday). state.selected is the selected index.\n// reset() restores default visuals and ARIA. destroy() removes listeners; repeated calls are safe.\n// Call destroy() before removing the root. A later mount starts with defaults.\n// Each controller owns its state. CSS-only motion responds to live reduced-motion changes.\n// Replace sample text with textContent and keep numeric labels/scales synchronized.\n// Bind --dd-surface, --dd-text, --dd-muted, --dd-accent, --dd-selected, --dd-edge and --dd-focus to your palette.\n","data-display-histogram-v11/example.js":"// Honest local demonstration: selections do not save, fetch or submit data.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application usage: mount a separate root with SLComponent.mount(root, { onChange(detail) {} }).\n// detail = { kind, action, state }; also emitted as bubbling, composed \"displaychange\".\n// select(index) accepts bins 0–5. setMode(\"frequency\" | \"cumulative\") changes the y-axis to 0–10 or 0–30 jobs; state contains selected and mode.\n// reset() restores default visuals and ARIA. destroy() removes listeners; repeated calls are safe.\n// Call destroy() before removing the root. A later mount starts with defaults.\n// Each controller owns its state. CSS-only motion responds to live reduced-motion changes.\n// Replace sample text with textContent and keep numeric labels/scales synchronized.\n// Bind --dd-surface, --dd-text, --dd-muted, --dd-accent, --dd-selected, --dd-edge and --dd-focus to your palette.\n","data-display-ranking-v11/example.js":"// Honest local demonstration: selections do not save, fetch or submit data.\nSLComponent.mountPreview(document.querySelector('.sl-component'));\n\n// Application usage: mount a separate root with SLComponent.mount(root, { onChange(detail) {} }).\n// detail = { kind, action, state }; also emitted as bubbling, composed \"displaychange\".\n// select(index) uses stable channel IDs 0 Direct, 1 Search, 2 Referral, 3 Social. setMode(\"visits\" | \"conversion\") sorts descending with stable ties. state contains selected and mode.\n// reset() restores default visuals and ARIA. destroy() removes listeners; repeated calls are safe.\n// Call destroy() before removing the root. A later mount starts with defaults.\n// Each controller owns its state. CSS-only motion responds to live reduced-motion changes.\n// Replace sample text with textContent and keep numeric labels/scales synchronized.\n// Bind --dd-surface, --dd-text, --dd-muted, --dd-accent, --dd-selected, --dd-edge and --dd-focus to your palette.\n"};
