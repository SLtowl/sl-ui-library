const field = MatteTextInput.mount(document.querySelector('#text-field'));

// Read the native input value in your form or application.
// field.reset() restores its default value; destroy() removes enhancements.
